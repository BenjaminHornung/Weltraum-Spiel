# Tasks

- [ ] 1. Capture the dirty-worktree baseline, correct invalid V2/design-QA claims, and validate the V3 spec/evidence protocol.
- [ ] 2. Implement typed commands, speed-profile plan metadata, preview provenance, pure lock admission, exact Engage, and focused runtime tests.
- [ ] 3. Promote the concept flight surface to normal `/`, preserve live WebGL/combat/debug/TestBridge boundaries, and implement runtime-owned HUD/radar values.
- [ ] 4. Implement the semantic modal planner, profile/preview/replan workflow, runtime SVG map, dynamic metrics/timeline, focus restoration, and input gating.
- [ ] 5. Update parity and command tests, add the functional planner E2E, run unit/build and all required regressions, and store fresh evidence.
- [ ] 6. Run strict Product Design comparisons at 1640x900 and 1280x720, fix every P0/P1/P2, run final review, and record readiness without committing or pushing.

## Completion constraint

Because Benjamin explicitly prohibited commits and pushes, executions and verification may be recorded, but task completion toggles, archival, and commit-dependent closure remain pending explicit authorization.
