# Tasks: Browser Celestial Gravity Core v1

- [x] 1. Freeze the versioning, stable-ID, physical-unit, parent-graph, epoch/frame, error-order, canonical-serialization and non-goal contracts in the change artifacts.
- [x] 2. Implement immutable celestial IDs, definitions, validation, canonical serialization, catalog indexes and the source-backed 15-body Aurelia starter catalog.
- [x] 3. Implement deterministic circular/bound-elliptic Kepler solving and analytical parent-relative plus absolute ephemeris state composition.
- [x] 4. Implement pure point-mass gravity, surface gravity, escape velocity and deterministic dominant-source selection with fail-closed minimum-radius behavior.
- [x] 5. Add focused unit tests for catalog contents/order/indexes, validation/error contracts, ephemerides, gravity, floating-origin invariance and deep immutability.
- [x] 6. Add the normal-route Vite dynamic-import Playwright test and generate task-owned deterministic JSON/Markdown evidence without screenshots.
- [x] 7. Run focused TypeScript/unit/browser verification, the full unit/build/E2E gates, `git diff --check` and an explicit allowed/forbidden-path scope audit.
- [ ] 8. Record final evidence and remaining limits, complete DevToolbox preflight/task toggles when path access permits, commit and push `feature/browser-celestial-gravity-core-v1` without merging to `main`.
