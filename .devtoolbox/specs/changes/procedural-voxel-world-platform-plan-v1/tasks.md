# Tasks

- [ ] Verify `origin/main` and all four remote Research branch SHAs, commit counts and exact file allowlists.
- [ ] Create the isolated planning worktree and integrate the four allowed Research commits individually.
- [ ] Reconcile existing concept and architecture authorities with the two requested Spielkonzept documents.
- [ ] Create the seven requested architecture documents with the binding representation, authority, persistence, worker and physics boundaries.
- [ ] Create the complete external-reference Adoption Matrix with bounded verdicts and Evidence caveats.
- [ ] Update the Living Master Plan with the current `origin/main` SHA and unique, valid-status work packages.
- [ ] Run deterministic Docs-only checks for scope, links, required decisions, IDs, statuses, Evidence language and copied-source risk.
- [ ] Record final Evidence, run available DevToolbox completion preflights without override, review the diff, commit logical slices, push and open a draft PR against `main`.

## Completion Policy

Task checkboxes are updated only after Evidence and a successful
`tasks_completion_preflight`. If the configured DevToolbox root guard rejects
the isolated worktree as `unauthorized_path`, the block is recorded and no
manual checkbox completion or override is used.
