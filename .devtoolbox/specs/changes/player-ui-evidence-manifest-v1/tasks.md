# Tasks

- [ ] Add the Player UI screenshot evidence manifest.
- [ ] Add an Editor test that validates manifest paths, PNG headers, dimensions, and file sizes.
- [ ] Run Unity MCP validation/test, explicit solution build, DevToolbox validation, and document findings.
- [ ] Commit and push only scoped evidence-manifest files.
