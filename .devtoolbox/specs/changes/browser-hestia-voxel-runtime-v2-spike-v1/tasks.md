# Tasks: Hestia Voxel Runtime V2 Spike V1

Tasks close only after fresh cited evidence and DevToolbox completion preflight.
The same single execution owns every phase, retry, review and verification.

## 1. Lock plan, spec and research boundary

- [x] 1.1 Validate proposal/design/spec/tasks, write the classified reference
  synthesis and architecture boundary, record concept image Git/SHA-256 hashes,
  and create exactly one execution.

## 2. Implement the sole authority and deterministic world

- [x] 2.1 Implement V2 constants, signed coordinates, immutable palette,
  byte signature, sparse chunk records and deterministic coast/river/terrace/
  strata/tree/flora generation with focused determinism/mapping tests.
- [x] 2.2 Implement ordered CAS `SubtractSphere`, dirty/affected chunk results,
  DDA and authority-backed AABB collision with focused negative tests.

## 3. Implement derived mesh and worker execution

- [x] 3.1 Implement deterministic material/AO-aware exposed-face greedy meshing
  over copied one-cell halos, including no-internal-face, seam and byte-identity
  tests.
- [x] 3.2 Implement the V2 module worker and bounded latest-revision scheduler
  with transferables, coalescing, stale-result rejection, disposal and telemetry
  tests.

## 4. Implement the executable V2 route

- [x] 4.1 Implement the exact query gate, dynamic bootstrap, Three-only adapter,
  static water, first-person fixed-step controller, authority collision, cutter,
  read-only diagnostics and complete lifecycle cleanup while preserving `/` and
  Surface Lab.

## 5. Prove browser behavior and visual result

- [x] 5.1 Add the exactly-once V2 E2E group entry and focused E2E using visible
  real controls/events to prove readiness, movement, fire feedback, one accepted
  edit, revision/adoption ordering, authority collision and TestBridge absence.
- [x] 5.2 Capture the five fixed 1920×1080 views and complete the explicit visual
  matrix for 0.25 m scale, coast/water logic, macro depth, strata, vegetation,
  block style and seam freedom.

## 6. Measure, review and close

- [x] 6.1 Run the production-build 1920×1080 warm benchmark and 100-cut stress,
  parse/store JSON evidence and issue an honest threshold-based decision.
- [ ] 6.2 Resolve independent technical-review findings, run all fresh Node-22
  focused/full verification and guards, reconcile test findings, complete final
  Plannotator review, then publish an unmerged normal PR only for `GO` or a
  clearly marked draft/failed-spike PR for a functional rejected result.
