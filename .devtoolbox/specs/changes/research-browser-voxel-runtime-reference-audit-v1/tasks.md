# Tasks

- [ ] Record immutable provenance, license path, inspected paths and execution status for all five named project entries.
- [ ] Complete the Voxelize mandatory source audit including workers, meshing, updates, persistence, tests, benchmarks, renderer coupling and local physics.
- [ ] Complete the Divine mandatory source audit including worker topology, shared-memory on/off behavior, schemas/models, renderer adapters and demos.
- [ ] Complete the AresRPG Engine/World and focused Veloren audits with authority, persistence, activity and maturity evidence.
- [ ] Perform required real-browser checks for every viable browser demo and keep screenshots outside the repository.
- [ ] Write the single research document with all requested sections, classified evidence, project matrix, clear Voxelize/Divine recommendations and at most three spikes.
- [ ] Record fresh verification, run available DevToolbox preflights without override, review the allowlist, commit and push only scoped research files.

## Completion State

The research actions are implemented, but task checkboxes remain open because
the configured DevToolbox path guard rejects the isolated worktree as
`unauthorized_path`. The blocking completion preflight was not bypassed and
`tasks_toggle` was therefore not called.
