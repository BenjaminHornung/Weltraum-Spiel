# Test Protocol: Browser Adaptive Microvoxel Authority V1

## Handoff boundary and deterministic evidence

This protocol governs the fresh Adaptive Continuation stage on
`feature/browser-structural-microvoxel-destruction-core-v1-continuation`.
Current Main is `f6d3fe69175b168ddea5e385c6d7b3452e6cba16`; Adaptive is
reconstructed only from
`4c1449e2bb0486fe59ee6b68da495c27abee21b5..5fb372bdba677e43e71566121c53efb5e244b93a`.
Historical runs are not current evidence. The sole shared-file exception is a
minimal edit to the current `package.json` that adds the Adaptive E2E spec
exactly once to `test:e2e:core`. No old package, dependency, lockfile,
bootstrap, Playwright configuration, CI, worker/streaming,
renderer/Presentation, Surface-Lab, or unrelated shared-file change is allowed.

Surface-Lab-/Voxel-Authority und Adaptive-Microvoxel-Authority
sind getrennte Domänen.

Keine von beiden darf ohne späteren Adaptervertrag
gleichzeitig World-Truth publizieren.

All protocol inputs are literal and deterministic. Evidence contains only
commands, exit statuses, resolved Node major/version, canonical inputs,
outputs, hashes, assertions, and review results. It contains no timestamps,
`generatedAt`, wall-clock values, random values, or claims dependent on runtime
duration. Run the browser proof twice and compare SHA-256 hashes of both
allowed JSON/Markdown evidence files; both runs must be byte-identical.

## Focused unit obligations

Each obligation requires a named focused unit test, literal deterministic
fixture/input, pass/fail result, and reproducible evidence. Renderer observation
or mock success is not authority evidence.

1. **Quantum/levels:** accept only 0.125 m and integer levels 0..4; reject
   invalid values before mutation.
2. **Brick shape:** accept exactly `16^3 = 4096` cells and reject wrong shape,
   channel, length, or level metadata.
3. **Ancestry-closed coordinates and geometry:** accept complete valid L0-L4
   relationships, negative aligned coordinates, and bounded safe `number`
   bounds/sphere geometry; reject alignment, half-open-bound,
   locally-safe/ancestor-unsafe, unsafe-intermediate, and non-safe cases without
   clamp/wrap/normalize or coordinate/gameplay/radius `BigInt`. Internal
   `BigInt` remains allowed only for unchanged FNV-1a64/canonical hashing.
4. **Parent/child partition:** prove all eight children exactly partition each
   parent with stable half-open bounds and no gap/overlap.
5. **Stable IDs:** reject empty, untrimmed, oversized, and unpaired surrogate
   IDs at construction with the identity error.
6. **Identity/metadata split:** request, worker, cache, telemetry, camera,
   renderer, timing, and completion order do not change authority values.
7. **Closed descriptor:** accept only deeply immutable `constant-v1` with fixed
   schema, identity/version, source revision, and validated finite sample;
   reject function-valued provider/sampler/callback/registry inputs.
8. **Descriptor binding:** descriptor digest changes authority, content,
   provenance, proof, snapshot, and plan bindings.
9. **Base authority:** reject absent, unsupported, malformed, or non-finite
   base data; infer no empty/air/default field.
10. **Immutable ordered journal:** append creates a snapshot; prior records and
    snapshots cannot be mutated; records apply exactly once in order.
11. **Journal rejection:** duplicate/ambiguous/unsupported/conflicting,
    out-of-range, malformed, or non-finite records fail closed.
12. **Canonical serialization:** prove fixed field/type order and stable
    encoding; reject unknown/duplicate/unsupported/non-finite input; exclude
    operational metadata and nondeterminism.
13. **Materialization:** equal key/descriptor/journal inputs produce identical
    bytes independent of request/cache/worker history; A-B-A rematerializes.
14. **Content/provenance hashes:** explicit FNV-1a64 V1 hashes are deterministic;
    changed authoritative inputs cannot reuse unrelated output.
15. **Literal independent vectors:** hard-coded expected UTF-8 bytes and hashes
    cover key, provider, journal, brick, provenance, snapshot projection, proof,
    accepted plan, and rejected plan; expected values are not generated by the
    production helper in assertions.
16. **Context/snapshot:** validate/deep-freeze authority context and inputs;
    prove the projection is canonical and excludes derived proof/digest fields.
17. **Opaque proof:** only the validation constructor issues a locally branded
    proof bound to key, content/provenance, descriptor, journal, revisions,
    planning epoch, and snapshot digest; structural/self-asserted receipts fail.
18. **Planner/fallback:** independently recompute snapshot digest; reject
    wrong-authority/key/provider/journal/revision/epoch/snapshot, forged, stale,
    absent, or malformed proof before selected/fallback coverage; no silent
    downgrade or replan.
19. **Budget/coverage:** required over-budget rejection is typed, deterministic,
    and side-effect empty; optional omission, atomic fallback, selected complete
    half-open gap-free coverage, and exact refinement replacement are proven.
20. **Collapse/eviction:** derived products only are released; descriptor,
    journal, provenance inputs, and identity remain; reload is idempotent and
    byte/hash/provenance-identical.
21. **Code-unit ordering:** multiple non-ASCII IDs prove one explicit
    code-unit comparator; `localeCompare`, host locale, and ICU do not affect
    projection order, canonical bytes, or signatures.
22. **Finite journal cap:** the named implementation constant is exercised at
    its boundary and over-cap journals fail before copy/sort/hash/materialize,
    with no input mutation or partial output.
23. **Finite snapshot/planner caps:** residents, active coverage, refinement
    requests, and aggregate derived work are exercised at each named boundary;
    over-cap values fail before copy/sort/hash/enumeration.
24. **Authority separation:** the Adaptive proof produces pure read-only
    results and does not mutate Surface-Lab/Voxel state, renderer truth, or
    product World-Truth.

## Literal pinned vector protocol

Use human-reviewed literal expected canonical UTF-8 bytes and FNV-1a64 V1
hashes for: key; `constant-v1` descriptor; immutable edit journal; materialized
brick content; provenance; non-circular snapshot projection; opaque proof;
accepted plan; and typed rejected plan. The fixture must state which fields are
included/excluded and must not call the production serializer/hash function to
produce its expected value. If a vector changes, stop until an intentional
schema/algorithm reason is recorded and reviewed; unexplained regeneration is
not acceptance.

## Required browser matrix

Run the existing real-browser proof through the normal `/` route, importing the
pure core and exposing read-only evidence. It must machine-prove:

- every L0-L4 cell/brick relationship, parent/child coverage, negative
  alignment, and half-open bound;
- deterministic descriptor variation and A-B-A rematerialization;
- ordered edit replay, immutable journal snapshots, and invalid edit rejection;
- valid proof use, plus stale/cancelled planning epoch and forged/mismatched
  proof isolation;
- required and optional requests, atomic fallback transition, and complete
  selected coverage;
- deterministic budget rejection with empty desired/keep/materialize/evict/
  fallback/coverage side-effect arrays;
- collapse and eviction while retaining descriptor/journal rematerialization
  authority;
- normal `/` behavior with no `window.TestBridge`, browser/page/console/request
  errors, or presentation authority;
- explicit code-unit ordering and finite-limit fail-closed cases;
- no Surface-Lab/Voxel mutation or concurrent World-Truth publication.

Evidence may claim only the verified current E2E-group membership, never
runtime integration, gameplay, rendering truth, or final Surface-Lab behavior. Screenshots are optional and only document a visible
proof harness state; they are not visual/product acceptance.

## Fresh Node 22 verification gate

Resolve and record the exact Node 22 executable; do not rely on an unverified
ambient Node. Run the complete matrix serially from
`apps/weltraum-browser`:

1. TypeScript no-emit validation.
2. `adaptiveMicrovoxelContracts.test.ts` alone.
3. `adaptiveMicrovoxelMaterialization.test.ts` alone.
4. `adaptiveMicrovoxelPlanner.test.ts` alone.
5. The Adaptive E2E spec once with retries disabled; preserve its evidence and
   signatures.
6. The same Adaptive E2E spec a second time with retries disabled; compare both
   JSON/Markdown evidence files and all recorded canonical signatures
   byte-for-byte.
7. Full unit suite.
8. Production build.
9. Current `test:e2e:core` group.
10. Exact E2E inventory: every current Main entry remains and Adaptive occurs
    exactly once in `test:e2e:core`, nowhere else.
11. Normal `/` route with no TestBridge.
12. Existing Hestia Surface-Lab E2E smoke.
13. Scope, package/lockfile, forbidden-import, nondeterminism, BigInt-use, and
    cross-authority audits.
14. DevToolbox verification linked to execution
    `bbca9639a41142918a493bee5173de45`.
15. Separate Adaptive completion preflight.

Use the existing local TypeScript, Vitest, Vite, and Playwright CLIs under the
verified Node 22 executable. Do not install or update packages. Browser retries
must be explicitly zero. Start only the browser server required by the current
Playwright configuration and verify its cleanup after each run.

## Static scans and exact scope audit

From the worktree root after implementation, classify rather than suppress
matches. The scans must find no authority use of nondeterminism, executable
provider input, stale receipt shape, renderer/worker/cache/timing authority
leakage, timestamps in evidence, or forbidden scope:

```powershell
rg -n --hidden --glob '!node_modules/**' --glob '!dist/**' --glob '!coverage/**' "Date\.now|new Date|Math\.random|performance\.now|crypto\.random|AdaptiveBaseFieldSampler|AdaptiveResidentValidationReceipt|function-valued|callback|receipt|localeCompare" apps/weltraum-browser/src apps/weltraum-browser/tests apps/weltraum-browser/evidence docs/browser-mainline
rg -n --hidden --glob '!node_modules/**' --glob '!dist/**' --glob '!coverage/**' "BigInt|[0-9]n|asUintN|three|window\.|document\." apps/weltraum-browser/src/voxel/adaptive
rg -n --hidden --glob '!node_modules/**' --glob '!dist/**' --glob '!coverage/**' "generatedAt|timestamp|0\.125|level|16|FNV-1a64|snapshot digest|planning epoch" apps/weltraum-browser/src apps/weltraum-browser/tests apps/weltraum-browser/evidence docs/browser-mainline
git diff --check
git status --short
git diff --name-only f6d3fe69175b168ddea5e385c6d7b3452e6cba16
```

The first scan is a classification gate: forbidden words may appear only in
explicit rejection tests/documentation, never as accepted authority inputs.
The second is informational and must agree with the contract tests. The final changed-path audit must include only the approved
implementation/test/fresh-evidence paths, these five artifacts, the Browser
documentation, and the minimal current `package.json` E2E-group edit. BigInt
matches are allowed only in unchanged canonical/FNV-1a64 hashing and explicit
rejection tests/docs; every other match fails.

## Ordered review and completion gate

After implementation: run the fresh serial matrix, browser proof twice, static
scans, and exact scope audit; obtain technical review and address confirmed
in-scope findings; rerun affected checks; run the separate Adaptive completion
preflight; then commit Adaptive as its own integration commit. Keep the change
active and unarchived. Do not toggle tasks before their fresh evidence exists.
Do not begin Structural before the Adaptive commit and stop-gate. Do not merge
to `main`.
