# Tasks

- [x] Implement and verify the complete surface resource extraction core V1.
  - Add the versioned immutable domain model, canonical signatures, scans, readiness evaluation, lifecycle transitions, deterministic pulses, Resource Core transfer integration, typed block reasons, fixtures, and mission/event intents under `apps/weltraum-browser/src/surface-extraction/**`.
  - Add focused unit coverage in `apps/weltraum-browser/tests/unit/surfaceExtraction*.test.ts`.
  - Add the normal-route port-5237 Playwright proof and config, deterministic JSON/Markdown evidence, and browser-mainline documentation in the approved write scope.
  - Verify spec validity, focused tests, browser proof twice byte-identically, browser health 0/0/0/0, forbidden-import/global constraints, immutability, scope allowlist, and dependency ancestry.
  - Commit as `#WELTRAUM-000 Add surface resource extraction core` and push non-force without opening or merging a PR.
