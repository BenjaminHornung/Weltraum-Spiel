# Tasks

- [ ] Add `PrototypePlayerRadarSnapshot` data collection for nav, arena, combat, docking, environment, obstacle, route, preview, and avoidance sources.
- [ ] Render typed minimap cues through the existing `PrototypePlayerHudRadarGraphic` and replace the fixed range label with snapshot auto-range text.
- [ ] Add/extend validation tests for snapshot content, range behavior, no IMGUI radar regression, and responsive panel layout.
- [ ] Capture Unity MCP verification evidence under this change and update the test protocol/findings.
