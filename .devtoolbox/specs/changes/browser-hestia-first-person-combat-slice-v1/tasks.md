# Tasks

Ownership rule: all work reuses DevToolbox execution
`c757ca664e5a4060b90a5ca665380bb6`. Do not create a second execution.
No task is toggled until its implementation, fresh evidence and completion
preflight all succeed. Manual rejection keeps every task open.

## Active delivery order — 2026-07-29, latest user priority

The user explicitly reprioritized execution without deleting any existing open
requirement. Task numbers below are an inventory, not the current run order.

1. Finish the already-running P0 liveness/performance slice: asynchronous
   revision-bound Structural preparation, automatic bounded continuation,
   adaptive Physics/Collision representation and recovery from finite
   `MotionBudgetExceeded` states. Fixed budgets may regulate one work batch,
   but may not permanently reject a valid finite object or cut.
2. Stop polishing the current placeholder world once that reusable
   Async/Adaptive basis and its real-browser gates are green.
3. Immediately implement the first Hestia Coast/Lush Biome and World-generator
   slice against the screenshots under `docs/Konzeptart/Hestia`, matching their
   look, feel, composition, vegetation language, atmosphere, lighting and
   terrain/water relationships as closely as the browser runtime permits.
4. Reproduce, persist and reprioritize Core bugs found against that new
   World/Design baseline. Existing findings remain open, but no additional
   legacy-only polish lane precedes the Biome slice.
5. Keep the SimCity-style World editor last. A free agent may perform only
   conflict-free preparatory research while higher-priority work is active.

Every stage still requires Unit tests, E2E, real browser playtests/screenshots
and explicit user manual verification. New user findings remain additive,
persistent and scheduled at the earliest suitable stage.

## Task 1 — Revised approval and contract boundary (Orchestrator)
- [ ] Preserve the failed screenshots and root-cause matrix as failed evidence.
- [ ] Obtain explicit user approval for the revised ExecPlan, the three public
  body/capacity rejection codes, two required-readiness source-breaking
  constructor migrations, bounded physics guardrails and finite Energy profile.
- [ ] Obtain separate explicit V3.1 approval for the canonical Structural
  detached-Fragment transfer, nested `authorityTransfer` and historic
  `bodySources`; no implementation before that approval.
- [ ] After approval only, apply the exact reviewed `Structural` hit/event,
  `BodyCapacityExceeded|ColliderBudgetExceeded|DetachedBodyImmutable`,
  readiness, transition/body/presentation/failure snapshot and port delta with
  the approved caps and Runtime-owned latest transition.
- [ ] Prove contract immutability/freeze behavior, stable identity and absence
  of Three.js/DOM/TestBridge dependencies.

## Task 2 — Dry Hestia footprint and world ownership
- [ ] Replace the two-brick origin fixture with the first valid candidate from
  the finite `x,z=-64..64 m` / `16 m` lattice ordered by squared distance,
  global `z`, global `x`; center the `64 x 32 x 64 m @ 0.50 m` footprint, use
  the exact center probe and calculate its vertical minimum as
  `O_y+32*floor((G_y-O_y-8)/32)`, rejecting an upper-reserve failure.
- [ ] Move water/shore/vegetation placement to immutable Runtime world facts.
- [ ] Admit the canonical `0.50 m` connected dry traversal component with
  `1.0 m` water clearance, `32 x 32 m` minimum footprint, `12 m` spawn-to-
  perimeter path, slope/step/capsule rules and typed fail-closed startup.
- [ ] Publish the Runtime-owned half-open `ShoreBoundary`; prove uphill/downhill
  water replay plus render/collision/residency parity and no visible edge.
- [ ] Bound the admitted test domain with one Runtime-owned, visually
  unobtrusive collision boundary; prove all four edges retain stable tick/frame
  time and cause no generation, remesh, collision-rebuild or allocation storm.

## Task 3 — Hestia lighting, materials and population presentation
- [ ] Make the Surface environment the sole Surface Play light-rig owner.
- [ ] Present readable terrain/water/vegetation depth and material separation
  from snapshots without Presentation-authored placement.
- [ ] Keep a deterministic spawn clearing/path and prevent opaque vegetation
  walls while preserving Hestia identity.
- [ ] Replace dark-hex/object-count assertions with ownership plus visual
  acceptance coverage.
- [ ] Replace the flat green/cyan sky treatment with the approved Hestia
  atmosphere/horizon and prove deterministic plausible tree height classes and
  connected, non-malformed silhouettes across the viewport matrix.
- [ ] After Core correctness and performance are green, run a separate
  screenshot-matched visual parity pass against the `23` PNG / `18` unique
  image reference set under protected-root `docs/Konzeptart/Hestia` and the
  cited readable physical voxel/destruction qualities of `Teardown`, using
  original Hestia assets and no Presentation-authored gameplay truth.
- [ ] Keep the additional depicted biomes in the later world roadmap and the
  depicted city as explicitly deferred hand-authored content; neither expands
  this bounded recovery slice into multi-biome streaming or procedural city
  generation.

## Task 4 — Supported locomotion rest
- [ ] Add internal unsupported, supported-moving and supported-resting states.
- [ ] Implement the approved `0.20 m/s` capture, `0.25 m/s` release and
  two-tick hysteresis transitions without snaps or blanket velocity zero.
- [ ] Add 600-tick `1e-6 m`/`1e-6 m/s` drift gates plus flat/slope/input,
  jump, threshold and external-impulse regressions.
- [ ] Correct and real-browser-test the camera-local lateral basis so `A` is
  left and `D` is right at multiple yaw angles.
- [ ] Reproduce one safe accepted Terrain crater and prove the atomically
  adopted collision revision lets the capsule walk/fall in, remain responsive,
  ground on the edited surface and leave by valid walk/slope/jump rules without
  slow sinking, sticky damping or repeated depenetration.

## Task 5 — Structural Umbrella Tree authority
- [ ] Selectively port and adapt historical Hestia structural vegetation from
  `897711024458062e3965c26cec05bd34c61d50db`.
- [ ] Compile stable Umbrella Tree graphs to current Adaptive/Structural
  occupancy, materials and root anchors.
- [ ] Publish immutable Structural object/component snapshots and prove equal
  seed/identity determinism.
- [ ] Keep Mist Sprouts/Caps explicitly non-solid and non-authoritative.

## Task 6 — Tree collision, Combat hit and Structural damage flow
- [ ] Add revision-bound Structural capsule and ray collision adapters.
- [ ] Resolve deterministic nearest Drone/Structural/Terrain hits through
  Combat Core and preserve semantic event ordering.
- [ ] Convert accepted Attached-tree damage to the approved half-even
  `SubtractSphere`, `radiusQuantum=3`, hit-material-filtered Structural edit.
- [ ] Prove the phase-2 fixture stays anchored after the first local hit and
  detaches by hit six; atomically reserve complete bodies/colliders before
  Combat shot cost/damage and prove capacity rejects use the public codes with
  only `FireRejected`, no shot debit/heat addition or authority effect, and a
  Runtime-published rejected latest transition,
  while preserving Terrain event/CAS/revision/hash/collision/remesh semantics.
- [ ] After V3.1 approval only, transfer exactly all detached Fragments from
  Damage source `r+1/e+1` into current Attached-only authority `r+2/e+2` using
  a separately validated/hashed Structural-Core command and unchanged Evidence
  V1.
- [ ] Prove hit 4 edits only the stump: no ghost cells/collision, duplicate
  Body or historic Body/Fragment/collider/mesh drift; include empty-current
  object, cold resolver, persistence/tamper and atomic budget regressions.
- [ ] Instrument real click receipt through hit/preflight/published result;
  valid Terrain and Tree cuts must complete promptly and a genuinely unsafe
  cut must reject promptly, typed and mutation-free without blocking the main
  thread or collapsing every target into one generic rejection.
- [ ] Eliminate the live approximately five-second stall on every Tree cut and
  prove a later valid stump cut remains exercisable without reload.
- [ ] Measure real Pointer-Lock input receipt through the first authoritative
  visible Tree edit. Provide immediate action acknowledgement within `100 ms`
  and publish the matching voxel/collision/presentation change within `250 ms`
  at p95 on the installed-Chrome test route; a fast unit phase or cosmetic
  impact effect cannot substitute for the visible authoritative change.

## Task 7 — Bounded Surface rigid-body physics
- [ ] Implement the approved `60 Hz` semi-implicit/quaternion, local-box-to-OBB,
  stable broadphase/SAT and sequential-impulse algorithm exactly.
- [ ] Start reserved detached bodies next tick; integrate Structural
  mass/COM/inertia, `1..4` bounded substeps and Terrain/body contacts.
- [ ] Enforce whole-batch `8` body/`64` box reservation and atomic scratch-tick
  `256` contact/motion/non-finite failure without clipping or partial publish.
- [ ] Keep Falling/Resting player/Beam collision and session-local Resting;
  reject Beam hits before firing as public `DetachedBodyImmutable` with no
  attempted-shot Energy debit/Heat addition, damage/edit or Terrain fallback
  while prior tick recovery/cooling remains; remove only on route cleanup.
- [ ] Prove real browser animation receives multiple distinct intermediate
  `Falling` poses/frames before `Resting`; a start-frame/end-frame-only fall is
  a failed performance result even when the deterministic end state is right.
- [ ] Reproduce and fix the live `MotionBudgetExceeded` failure in which the
  detached Tree body misses Terrain contact, falls below the local surface and
  eventually stops all Surface physics. Correct contact/frame ownership or
  tunneling before changing any motion cap.
- [ ] Prove a route-realistic detached Tree reaches Terrain contact and
  `Resting` without `physicsFailure`, below-ground pose, pointer-lock release or
  simulation stop; record intermediate poses, contact tick, rest tick and
  fixed-step/frame timing in the real browser.
## Task 8 — Finite Energy and Combat-derived readiness
- [ ] Implement 240 J maximum, unchanged 12 J cost, delayed 12 J/s fixed-tick
  recovery after 3.0 s and full route/re-entry initialization.
- [ ] Publish Combat-derived typed readiness/recovery; preserve
  `EnergyInsufficient`; rejection adds no shot debit/heat or damage/edit.
- [ ] Prove 20 accepts, 21st rejection, exact one-shot recovery, cap and
  deterministic reload/re-entry behavior.

## Task 9 — Player HUD and failure copy
- [ ] Consolidate persistent suit/weapon state into edge-bound player UI and
  keep the center safe for reticle plus one target context.
- [ ] Add one bounded transient action zone and one warning/next-action zone
  with deterministic lifetime and no overlap.
- [ ] Separate Energy from Heat and pin the general body-capacity copy plus
  collider-complexity, detached-body and stale-refusal copy without raw enums,
  exceptions, paths or debug telemetry.
- [ ] Prove ARIA values/live regions, focus order, 200% text zoom, reduced
  motion, contrast and responsive bounding boxes.
- [ ] Add an accessible startup loading state owned by bootstrap/readiness,
  visible from route entry until playable or typed failure; never leave a blank
  canvas or silently replace a failure with an endless loader.
- [ ] Derive labels and any determinate progress only from real finite startup
  phases. Unknown-duration work uses an honest indeterminate state; prove
  monotonic phase order, ready/failure cleanup, reload and reduced-motion paths.
- [ ] When finite startup work is measurable, render a real left-to-right
  progress bar whose fill and accessible value equal the computed `0..100%`;
  never substitute a looping decorative animation for measured progress.

## Task 10 — Integration, route and cleanup
- [ ] Integrate World, Player, Combat, Structural, Physics and Presentation in
  the approved order: pure Structural preview and whole-batch reservation
  before accept/shot-cost/damage, preflight rejection without shot debit/heat or authority effect, one CAS adoption,
  atomic collision/body publish, then next-tick Physics.
- [ ] Preserve exact query precedence, Surface Lab behavior, mode-owned input
  and `window.TestBridge` absence.
- [ ] Prove pagehide, pointer-lock loss, partial startup failure and repeated
  disposal clean every owned listener, body, artifact and renderer resource.
- [ ] Prove current-tree/body contact and genuine authority/collision revision
  swaps cannot permanently pause movement or simulation; matching snapshots
  recover automatically on the next valid tick, including one deliberate HMR
  replay without weakening fail-closed stale-revision checks.
- [ ] Reproduce the retained `VoxelMeshProduct is invalid` failure and capture
  its exact Surface-Nets validation issue plus triggering edit; distinguish
  edited-Terrain degeneracy from an incoherent HMR module graph, and prove that
  neither leaves repeated fire input, invalid mesh state or a frozen route.

## Task 11 — Fresh regression, runtime and screenshot evidence
- [ ] Run focused and complete unit suites, production build, Surface Play E2E
  and relevant grouped/full E2E inventories using installed Chrome.
- [ ] Reproduce the live post-edit crater path with real pointer-lock input and
  prove `AIRBORNE` transitions to stable `GROUNDED` support on the edited floor
  before jump/exit; direct-state unit evidence cannot override a live failure.
- [ ] Capture the specified six-viewport matrix for spawn, water replay, idle
  slope, vegetation, target, Terrain edit, Energy, Tree hit/fall/rest,
  pointer-lock, failure and debug-separation states.
- [ ] Pair screenshots with seed/anchor/bounds, camera/water/ground values,
  ticks, revisions, lifecycle, collision and cleanup evidence.
- [ ] Use real pointer-lock keyboard/mouse input to replay A/D, accepted and
  unsafe clicks, post-edit crater entry/exit/jump, tree-contact liveness and
  every bounded edge; attach action latency plus fixed-tick/browser-frame
  timing evidence.
- [ ] Run diff/scope/contract/import/global/secret scans and document `.NET` as
  NOT APPLICABLE while the worktree contains no solution.

## Task 12 — Manual gate and closeout
- [ ] Start exactly one managed strict-port Vite server only after automated
  gates pass, open installed Chrome and hand the real route to the user.
- [ ] Record explicit user acceptance or exact rejection; never infer success.
- [ ] After acceptance only, run findings-first technical review, resolve
  findings, rerun affected/full verification and DevToolbox preflight.
- [ ] Toggle only evidence-backed tasks, then invoke final Plannotator human
  diff review; no commit or non-force push before that approval.
- [ ] Do not create a PR, merge main, archive the change, deploy or release.

## Parallel Task 13 — Creative/City-Builder authoring decision package

This planning lane may run while Core fixes are in progress, but it does not
expand the approved recovery runtime. Implementation requires a separately
reviewed and explicitly approved ExecPlan.

- [ ] Inventory existing WorldTemplate/WorldInstance, SurfaceLocalFrame,
  authored-socket, manifest, representation and editor/debug-route patterns
  before proposing a new owner or file format.
- [ ] Define a separate authoring surface with selection, transform gizmos,
  grid/socket snapping, layers, palette, duplicate/delete, undo/redo, preview,
  validation and deterministic save/load; Creative operations must never be
  exposed as normal Surface Play commands.
- [ ] Define one diff-friendly, versioned authored-world overlay as the only
  accepted editor output. Three.js objects, DOM state and editor selection are
  projections and never become World truth.
- [ ] Specify human-confirmed agent/procedural suggestions for roads, parcels,
  repetition and vegetation. Suggestions remain previews until explicitly
  accepted, and accepted output records seed, tool version and provenance.
- [ ] Split an MVP and later roadmap, name exact owners/files/tests/budgets and
  present the contract/public-behavior delta to the user before implementation.

## Parallel Task 14 — Biome and modular Building-Kit preparation

- [ ] Define a reusable Building-Kit palette with stable asset IDs, variants,
  footprints, sockets, clearance, terrain fit, LOD/representation policy,
  collision/destruction class and provenance without making meshes authoritative.
- [ ] Define authored roads, parcels, districts, biome masks, exclusions and
  hero-site locks so semi-procedural helpers cannot overwrite intentional
  layout or story/gameplay semantics.
- [ ] Design deterministic validation and fixture coverage for overlap,
  unsupported placement, blocked traversal, shoreline/terrain conflicts,
  missing assets, budget overflow, serialization round trips and stable hashes.
- [ ] Prepare one tiny future proof fixture: a Coast/Lush blockout containing a
  road segment, parcel, two modular buildings, vegetation/exclusion mask and
  one authored hero socket. Do not implement or load it in the current runtime
  before the separate authoring plan is approved.

## Parallel Task 15 — Frequent unsafe Terrain rejection and editable fallen bodies

The frequent Terrain rejection is a current defect investigation. Editable
Falling/Resting bodies supersede the approved V3.1 `DetachedBodyImmutable`
behavior and require a separate reviewed V3.2 decision before implementation.

- [ ] Preserve and reproduce the failed browser evidence where ordinary clicks
  frequently report `The terrain cut would make this test area unsafe` away
  from an obvious Dry boundary. Trace nearest-hit classification, quantized hit
  and radius, immutable Dry mask, revalidated Traversal component and exact
  rejection source; never weaken legitimate boundary safety to hide the bug.
- [ ] Prove clearly interior valid Terrain cuts accept promptly and only cuts
  that actually violate the admitted Dry/traversal invariant reject promptly
  with accurate target-specific copy, unchanged Energy/Heat and no mutation.
- [ ] Produce a V3.2 contract/authority plan for voxel edits on both `Falling`
  and `Resting` detached bodies: body-local ray hit, Combat damage, Structural
  edit, split/mass/inertia/collider rebuild, world-pose and momentum transfer,
  capacity reservation and atomic immutable snapshot publication.
- [ ] Pin hitbox/collision and destruction acceptance: no ghost cells, stale
  body collider, fall-through to Terrain, duplicate body, tunneling or
  Presentation-owned edit; repeated real-browser cuts remain responsive.
- [ ] Present the exact replacement/narrowing of `DetachedBodyImmutable`, new
  body-source/current-state ownership, fragment lifecycle/capacity policy and
  public contract/migration impacts for explicit user approval before code.

## Task 16 — Resting-body Player collision and escape

- [ ] Preserve the live failed evidence where the player is position-fixed
  inside a real `Resting` Tree body while `AIRBORNE`/`Unsupported` and vertical
  velocity continues below `-1100 m/s`; keep the body pose, player state,
  revisions and screenshot bound to one runtime.
- [ ] Add a deterministic RED regression that reproduces blocked Capsule motion
  plus unsupported gravity accumulation against a pose-bound Dynamic Body and
  proves the first incorrect velocity/contact transition without a blanket
  velocity clamp or zero.
- [ ] Fix Dynamic-Body Capsule overlap/contact resolution so collision remains
  authoritative, overlap is prevented or deterministically depenetrated,
  contact-normal velocity is resolved and tick/velocity state stays finite and
  bounded while tangential escape remains responsive.
- [ ] Real-browser test a Falling body approaching the player and Resting-body
  contact from multiple faces; prove no trap, stable tick progress, lateral
  escape, grounded re-acquisition, jump and no pointer-lock/failure state.

## Task 17 — Toggleable performance-debug overlay

- [ ] Define and document one keyboard toggle plus an exact read-only field
  allowlist: FPS; current/rolling p50/p95/max frame time; bounded scrolling
  graph; fixed tick/revisions; body lifecycle/count; last action; Long Tasks;
  and available contact/substep/click-latency diagnostics.
- [ ] Keep the overlay absent from the normal HUD while disabled, Projection-only
  while enabled, strictly bounded and low-overhead; it owns no gameplay state or
  commands, changes no fixed-tick ordering and keeps `window.TestBridge` absent.
- [ ] Prove toggle/focus/reload/cleanup behavior, bounded buffer size, exact
  value provenance and an approved measured self-overhead in real Chrome.

## Task 18 — Stock-Tree destruction liveness and collider representation

- [ ] Pin a deterministic repeated-cut Stock-Tree progression that reproduces
  the user-observed `ColliderBudgetExceeded` / `This cut is too complex to
  simulate.` dead end, including every accepted hit, Structural revision/hash,
  detached Component, body ID, occupied-cell count and compacted-collider
  digest.
- [ ] Keep deterministic multi-axis box partitioning only as the exact fast
  path. If a prospective Stock-Tree body exceeds it, derive one
  revision/hash-bound sparse or hierarchical adaptive collider for the same
  rigid Component, lowering Physics/Collision voxel resolution when necessary
  while retaining the exact high-resolution Structural Authority. No valid cut
  may remain blocked by a fixed collider-count limit.
- [ ] Prove that every valid local cut in the pinned progression accepts,
  publishes atomically, remains further editable where the approved contract
  permits it, falls and rests without duplicate/ghost cells, and never becomes
  permanently non-destructible because of a representation artifact.

## Task 19 — Bounded asynchronous Structural preparation

- [ ] **Live finding 2026-07-31 — Async preparation is still not responsive in real Chrome:** with `?surfacePlay=1&structuralFire=async-v2`, a real Tree hit stayed in `PREPARING · RUNNING`/`ASYNC RUNNING` for tens of seconds, then reached `ASYNC READYTOADOPT` with `RUNNING 0` while HUD latency continued growing (`45,976.60 ms`, later `132,665.10 ms`). The same run exposed frame `MAX 233.30 ms` and Long Task `MAX 12,326 ms`. Trace the first main-thread blocking operation and freeze terminal latency at `ReadyToAdopt`/failure; seed packaging before `client.run` must move off Main or prove bounded. Repeated real Chrome hits must satisfy the existing frame/Long-Task gates.


- [ ] Profile the exact current candidate after the persistent Connectivity
  index and attribute remaining synchronous cost to Connectivity, mass,
  transfer, collider, hashing and collision publication before introducing a
  Worker or staged command.
- [ ] Prepare only immutable revision/hash-bound Structural candidates off the
  main thread with bounded queue length, deterministic ordering, stale-result
  cancellation, bounded work batches with continuation or deterministic
  adaptive-representation fallback, and failure-atomic adoption at a fixed
  tick; never expose a partial Authority, Collision, Physics or Presentation
  state.
- [ ] Publish immediate typed input feedback and expose queue depth, prepare
  latency, transfer cost and publish latency to the F1 overlay. Prove no
  starvation, no unbounded backlog and no worker result can mutate a newer
  Authority revision.
- [ ] Size worker concurrency from measured capability and leave the main/render
  thread responsive: use otherwise-idle CPU cores for deterministic preparation,
  use GPU work only for suitable derived presentation/mesh workloads, and never
  move Structural Authority, canonical hashes or nondeterministic Physics truth
  to GPU compute merely to consume available resources.
- [ ] Real-browser gate repeated Terrain/Tree cuts with click feedback at or
  below 100 ms, click-to-authoritative-publication p95 at or below 250 ms and
  no Long Task above 100 ms while `window.TestBridge` remains absent.

## Task 20 — Three-body Surface-Physics failure discriminator

- [ ] Reproduce the user-observed `1 Resting + 2 Falling` Tree-body state in a
  deterministic Authority/Terrain fixture and replay the exact Physics input
  twice byte-identically.
- [ ] Record the typed failure code and stage, body IDs/revisions/collider
  counts, finite pose/velocity facts, required translation/rotation substeps,
  per-substep and cumulative contacts, and first non-finite field on every
  bounded failure exit.
- [ ] Use the now typed single-body `MotionBudgetExceeded` evidence plus
  owner-bound step diagnostics to replace the fixed fatal substep cap with
  deterministic adaptive substeps, CCD or worker continuation. Work limits may
  bound one batch, but no finite valid body may permanently stop; velocity
  zeroing, blind clamping and untyped fixes remain forbidden.
- [ ] Fresh-browser replay after the last source reload must capture the same
  typed diagnostics before any HMR/navigation and prove responsive recovery
  through repeated branch cuts without a permanent Surface-Physics stop.

## Task 21 — Adaptive Physics representation and liveness

- [ ] Keep high-resolution Structural Voxel Authority, edits, revisions, hashes,
  mass and support truth exact while deriving Physics/Collision representations
  through the integrated Representation Ladder.
- [ ] Select deterministic representation levels from measured complexity and
  required contact fidelity: exact compact boxes first, then sparse or
  hierarchical occupancy with locally refined impact/contact zones and a
  coarser bounded fallback for very large or fragmented bodies.
- [ ] Treat collider, contact and substep limits as scheduler work-batch controls
  with continuation, cancellation and stale-result rejection, never as a
  permanent `ColliderBudgetExceeded`, `ContactBudgetExceeded`,
  `MotionBudgetExceeded` or “too complex” gameplay dead end for finite valid
  state.
- [ ] Prove tiny, stock, heavily fragmented and deliberately oversized
  deterministic fixtures preserve one rigid body per connected Component,
  remain editable, collide without trapping/tunneling, fall and rest, and
  produce byte-identical Authority plus representation-level evidence across
  repeated synchronous-reference and worker runs.

## Later Task 22 — Authoritative voxel-terrain support and fracture

Scheduled only after Coast/Lush B blockier-design acceptance and Task 19
PrepareSeed liveness gates. The authoritative contract is
specs/unified-surface-voxel-authority/spec.md; no competing terrain-only or
tree-only model may be created. This is not current Phase 4C work and does not
block Coast/Lush delivery.

- [ ] Compile procedural terrain and authored Tree graphs into one immutable,
  revisioned Surface-Voxel Authority. Keep the Tree graph as provenance/source
  evidence after compilation; reuse existing revision, CAS, density/material,
  connectivity and hash helpers.
- [ ] Define the common half-even 0.125 m SurfaceEditCommand boundary and a
  versioned material table for mass density, cohesion, strength and support
  capacity/span. Pin exact values only in a separate approved decision record.
- [ ] Derive mesh, static collision, beam queries, mass/COM/inertia and dynamic
  body sources from the same authority revision/content hash. Permit adaptive
  per-brick or local collision representation; do not introduce a global
  0.125 m grid.
- [ ] Implement deterministic affected-neighborhood/support-frontier traversal,
  six-neighbor components and support roots. Prove supported, undercut,
  disconnected and material-overload fixtures with no floating presentation
  block, hidden teleport or clipped collider.
- [ ] Implement whole-batch fracture/body admission and fall/rest evidence.
  Fixed budgets are work-batch controls with continuation/adaptive fallback;
  no finite valid component is permanently rejected only by representation
  complexity. Preserve atomic no-partial-publication semantics.
- [ ] Move source snapshot, seed packaging, authority derivation and fracture
  preparation off Main. Main validates source revision/hash and atomically adopts
  one complete result or typed refusal; stale/cancelled/restarted workers leave
  no mutation.
- [ ] Capture browser evidence for repeated Terrain/Tree cuts, intermediate
  Falling frames, Resting state and the supplied undercut-block reproduction.
  Keep this task open until Coast visual gates, async liveness and authority
  parity are all green.

### Task 22 bounded sequential proof subtasks — Phase 2 proof retained; Phase 2 gate reopened pending Phase 1 baseline; Phase 3+ open

Phase 2 proof implementation and evidence exist, but its sequential gate is
reopened pending the Phase 1 baseline. Phase 3 is open, has no implementation
claimed here, and is gated by
the separately approved, named material-rule record `Hestia Unified Surface
Material Rules V1`, which is hash-bound in the evidence and supplies the
versioned density/support-capacity/span/anchor/overload/fracture rules. Phase 3
does not require cohesion, strength or stress mechanics; those remain deferred.
Phase 2 may prove wiring and existing-class behavior but may not choose those
values. Preserve execution `c757ca664e5a4060b90a5ca665380bb6`.

- [ ] **22.1 Baseline/spec gate:** Record HEAD, source/worktree hashes, current
  V1 Terrain/Tree parity, byte-valid Adaptive L0-L4 key/hash behavior,
  PrepareSeed worker-start-before-packaging trace, and Coast identity,
  vertical-band and R09-R13 host-cap baseline. The reviewed five-artifact
  handoff baseline is:

  | artifact | SHA-256 | reviewed status |
  | --- | --- | --- |
  | `.devtoolbox/specs/changes/browser-hestia-first-person-combat-slice-v1/tasks.md` | `84AE96D1BEF598B7EAD89008771496C694F3F56D3F77A1B63B9B39F07A29A805` | ` M` |
  | `.devtoolbox/specs/changes/browser-hestia-first-person-combat-slice-v1/proposal.md` | `5A0CDF6280B6D1E7A90594E1C423984C69A0AD8695EDF8DC999068263C98C5AB` | ` M` |
  | `.devtoolbox/specs/changes/browser-hestia-first-person-combat-slice-v1/design.md` | `C2106E459759F35D0CA6995C266A432B1AE4FD07B5CA6BE173B345D8F7E9A766` | ` M` |
  | `.devtoolbox/specs/changes/browser-hestia-first-person-combat-slice-v1/specs/unified-surface-voxel-authority/spec.md` | `D9D2844EABC34172B2E432A5E46F1365E999659079937F2FF997B3E3F6F8B656` | `??` |
  | `docs/browser-mainline/hestia-unified-adaptive-brick-authority-v1-execplan.md` | `8548F94F8C5C46753C2BA785C4FB6096B9D3D75DCCF0DE87C1AD0971237E1C94` | `??` |

  A `??` artifact is guarded by its recorded SHA-256 just like a tracked
  artifact; any status or hash drift stops the phase before mutation. Stop
  before mutation if any source artifact or dirty-worktree guard changes
  unexpectedly.
- [ ] **22.2 Isolated mixed-resolution authority fixture:** Compile deterministic
  mixed-material terrain, trunk, fracture and vegetation inputs into exactly one
  immutable authority snapshot covering vegetation at every existing `L0-L4`
  level, with ground at L2/L3 and trunk/fracture at L4. The canonical snapshot
  owns occupancy, density, material, ordered edits/provenance, identity,
  revision and content hash; render, collision, support, mass and physics are
  derived products and never alternate authority. Main owns the snapshot,
  command acceptance/CAS and atomic adoption; workers (if exercised) own only
  private derivation. Bind `protocol/schemaVersion`,
  `derivationAlgorithmVersion` and `materialTableVersion` into the snapshot,
  command and result commitments and validate exact equality at adoption.
  Keep it isolated and production-unwired: prove no import or caller from the
  live Coast/Surface Play route reaches the fixture, and do not promote
  optional L5 without the later same-camera A/B gate.

  Phase 2 owns exactly these existing source targets:
  `apps/weltraum-browser/src/voxel/adaptive/types.ts`,
  `apps/weltraum-browser/src/voxel/adaptive/canonical.ts`,
  `apps/weltraum-browser/src/voxel/adaptive/validation.ts`,
  `apps/weltraum-browser/src/voxel/adaptive/materialization.ts`,
  `apps/weltraum-browser/src/voxel/structural/model.ts`,
  `apps/weltraum-browser/src/voxel/structural/canonical.ts` and
  `apps/weltraum-browser/src/voxel/structural/persistence.ts`. It owns exactly
  these focused test targets:
  `apps/weltraum-browser/tests/unit/adaptiveMicrovoxelContracts.test.ts`,
  `apps/weltraum-browser/tests/unit/adaptiveMicrovoxelMaterialization.test.ts`
  and
  `apps/weltraum-browser/tests/unit/structuralMicrovoxelContracts.test.ts`;
   extend these existing tests rather than creating a parallel fixture/test
   surface. No other production or test path is owned by Phase 2.

   Implementation evidence is retained; sequential completion awaits the Phase 1 gate.

  Run from the exact cwd
  `C:\IFI_SourceCode\Temp\WeltraumSpiel\.worktrees\Weltraum-Browser-IFIWELTRAUM-000-browser-hestia-first-person-combat-integration-v1\apps\weltraum-browser`
  with Node 22:

  ```powershell
  node --version
  node .\node_modules\vitest\vitest.mjs run tests/unit/adaptiveMicrovoxelContracts.test.ts tests/unit/adaptiveMicrovoxelMaterialization.test.ts tests/unit/structuralMicrovoxelContracts.test.ts
  node .\node_modules\typescript\bin\tsc -p tsconfig.json --noEmit
  node .\node_modules\vite\bin\vite.js build
  Set-Location '..\..'
  git diff --check -- .devtoolbox/specs/changes/browser-hestia-first-person-combat-slice-v1/tasks.md docs/browser-mainline/hestia-unified-adaptive-brick-authority-v1-execplan.md
  git diff --name-only -- .devtoolbox/specs/changes/browser-hestia-first-person-combat-slice-v1/tasks.md docs/browser-mainline/hestia-unified-adaptive-brick-authority-v1-execplan.md
  git status --short --untracked-files=all -- .devtoolbox/specs/changes/browser-hestia-first-person-combat-slice-v1/tasks.md docs/browser-mainline/hestia-unified-adaptive-brick-authority-v1-execplan.md
  ```

  Record all command exit codes and evidence under
  `.devtoolbox/specs/changes/browser-hestia-first-person-combat-slice-v1/evidence/task-22/phase-2`.
- [ ] **22.3 Deterministic derivation, budget and mixed-material gate:** Prove edit quantization/CAS/hash,
  support roots, affected-neighborhood traversal, six-neighbor components,
  undercut/disconnected/overload fracture, body-source identity and legal
  parent coalescing byte-identically across repeats. Reject coalescing across
  edit/fracture/structural-class boundaries and enforce the one-level neighbor
  rule plus coarse-owned 2:1 transition strip. Add half-open seam coverage:
  coarse-face ownership at both half-open sides, and deterministic
  lexicographic ownership for edge/corner seams, including exact boundary
  points and the adjacent cells. This phase is unblocked by
  `docs/browser-mainline/hestia-unified-surface-material-rules-v1.md` with
  SHA-256 `900AC600C769C7B0A4312BBD00642230124E842C3C999077141DAB344A531C33`.
  Derive one immutable material table with the exact raw mappings: Voxel byte
  `0` `SolidRock` -> `Rock`; Structural uint16 `0` `Air`; Structural uint16
  `1/2/3` are the existing `Root`/`Wood`/`Canopy` raw IDs and are never
  equated with Voxel IDs. Bind the approved
  `materialTableVersion: hestia.unified-surface-material-rules.v1` and the
  canonical `materialTableContentHash` into snapshot, command and result using
  the existing canonical owner/`hashAdaptiveCanonical` hasher. Bridge the current adaptive table
  version through that owner and rerun the Phase 2 parity tests before
  adoption. The gate requires only density, support-contact capacity, maximum
  span, explicit anchors, overload and fracture policy; cohesion, strength and
  stress remain deferred. Use 8 dynamic bodies total, 64 colliders per body at
  admission and 256 contacts per outer simulation tick at the solver stage.
  Attempt adaptive continuation/refinement before final typed whole-result
  refusal where existing policy allows; publish no partial result. For each
  local cantilever run, use the minimum approved span across all participating
  occupied/supporting material groups and take capacity from the
  supporting-side frontier. Implementation and evidence remain open.
 - [ ] **22.4 Dirty typed-array delta gate:** The smallest future
   production-unwired Adaptive contract target is
   `apps/weltraum-browser/src/voxel/adaptive/dirtyDeltaProtocol.ts`; it owns
   only the delta schema, typed-array descriptor, and revision/hash/version
   validation, reusing Adaptive canonical owners. Do not claim that file exists
   or implement it in this task. Otherwise extend existing worker/adaptive
   tests; a dedicated `tests/unit/adaptiveDirtyDeltaProtocol.test.ts` is
   allowed only if Phase 4 later needs a separate host. Transfer the seed once,
   then only typed-array dirty-brick deltas through the existing
   `StableWorkerJobQueue`/gate and 16 MiB batch. Reuse the existing generic
   owners `src/workers/queue.ts`, `src/workers/workerPool.ts`,
   `src/workers/protocol.ts` and `src/workers/resultGate.ts`: preserve queue
   admission decisions `RejectedQueueFull` and `RejectedDuplicateJob`,
   lifecycle/cancellation/transfer/release semantics, and stale-result
   rejection. Prove predecessor/result revision and hash, authority identity,
   epoch, cancellation, release telemetry, bounded backpressure and atomic
   adoption. `src/surface-play/workers/preparedStructuralFire*` remains
   compatibility/shadow evidence only until delta parity; the Phase 4 first
   proof must avoid touching those untracked files and remain
   production-unwired. Record enqueue, rejection, cancellation and release
   counts/bytes. Use 16 MiB pages and a 64 MiB retained-result cap. An
   oversized logical delta continues across pages, but Main adopts only one
   complete, validated result after all pages arrive; no page is independently
   authoritative. Measure actual payload bytes and do not claim 64 KiB when the
   materialized estimate is 128 KiB.
- [ ] **22.5 Surface-Lab LOD/seam render A/B gate:** From one authority revision,
  prove Three.js WebGL BufferGeometry/batching for ground L2/L3 and trunk/
  fracture L4, deterministic coarse-owned blocky 2:1 seam strips, and no
  gameplay truth in render products. Test optional local L5 only as a measured
  same-camera A/B for fine vegetation after L0-L4 parity is green; reject it
  unless same-camera visual parity improves while remaining within the existing
  `16 MiB` page, `64 MiB` retained-result, `64`-collider, no Long Task
  `>100 ms` and `p95 <=250 ms` publication ceilings. L5 must improve parity,
  not merely reduce memory, worker or collider cost.
- [ ] **22.6 Active physics proof:** Prove exact interaction products at the
  existing 32 m tier and sleeping/exact policy at 96 m use the same authority
  revision/hash for dirty updates and stale refusal. Capture active contact,
  fall/rest and supplied undercut-block evidence without clipped colliders,
  hidden teleports, partial publication or velocity-zero correction.
- [ ] **22.7 Coast/tree shadow parity:** Keep current Coast routes/adapters live;
  compare identity, vertical band, authority/hash, LOD, render, collision and
  physics products against the old path. Resolve R09-R13 scope/host-cap issues
  in shadow only and capture fresh browser evidence before any live cutover.
- [ ] **22.8 Compatibility cutover/removal gate:** After shadow parity, adopt the
  delta path with a rollback-safe prior snapshot, retain byte-compatible L0-L4
  keys/hashes and remove the old PreparedFire full-state path last. Prove no
  same-world silent switch, no stale mutation and no unapproved public-contract
  change; keep all failed evidence and the prior path until the gate passes.
- [ ] **22.9 R09-R13 final visual/performance parity:** Run the fixed Chrome
  viewport/screenshot matrix, repeated Terrain/Tree cuts and final authority/hash
  parity. Record acknowledgement, publication, adoption, dirty-payload and
  Long-Task metrics; close no Task 22 subtask until all quantitative gates,
  deterministic replay and user-visible evidence are fresh and green.

Current blockers are recorded, not hidden: focused PrepareSeed tests may be
green but live Chrome proof is pending; Coast identity/vertical-band/R09-R13
scope/host-cap corrections remain before Coast implementation; fracture stays
deferred until the Coast/Lush blocky world is visible.
