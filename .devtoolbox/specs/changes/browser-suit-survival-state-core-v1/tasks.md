# Tasks

Each task is bounded by the proposal/spec/design and the eventual implementation allowlist. Report format for every task: changed files; acceptance coverage; commands/scenarios and results; blockers; remaining risk/unverified. Stop immediately for forbidden-path need, authority contradiction, or concurrent edit.

## 1. Validate specification readiness

- [x] **Objective:** prove the five artifacts are internally complete before implementation. **Files/search targets:** this change directory only; search requirements, IDs, time/order, allowlist, user checks 1–32, protocol. **Acceptance:** `specs_validate` succeeds and no unresolved contract gap exists. **Guidance:** read-only; do not reinterpret domain ownership. **Skills/MCPs:** `devtoolbox-specs-execution`; DevToolbox `specs_validate` by orchestrator. **Verification:** validation plus heading/keyword completeness check. **Report:** standard format above with validation evidence. **Stopping rule:** stop on validation error or contradiction; do not implement.

## 2. Implement validated values and immutable models

- [x] **Objective:** add identities, scalar validators, errors, canonical serialization/signatures, definitions, state/exposure, workload/mode/subsystem registries, and factories. **Files/search targets:** existing local deterministic core utilities first; then `apps/weltraum-browser/src/suit/**`; `tests/unit/suit*.test.ts`. **Acceptance:** requirements for exact IDs, safe integers/`-0`, models, defensive copy/sort/freeze, errors, closed registries, and tests 1–8/28 pass. **Guidance:** reuse fitting local canonical utilities; no floating implicit rounding or external owner import. **Skills/MCPs:** implementation worker; no Unity/browser MCP. **Verification:** `npx vitest run tests/unit/suit*.test.ts --maxWorkers=1` with relevant concrete files. **Report:** standard format plus reused utilities. **Stopping rule:** stop if reuse requires edits outside allowlist or model authority is ambiguous.

## 3. Implement commands and exact transition pipeline

- [x] **Objective:** implement command types/results, CAS/deduplication, fixed 10 Hz stepping, remainder arithmetic, and exact phase order. **Files/search targets:** `src/suit/**`, focused suit command/simulation tests. **Acceptance:** exact revision/tick semantics; immutable deterministic out-of-window rejection; whole-schedule duplicate-ID pre-rejection before mutation; `NoChange`; actual accepted resupply/recharge; commands execute once immediately before their designated in-window tick; equivalent aggregate/repeated schedules have byte-identical state/events/results; order invariance; and tests 5–13/27 pass. **Guidance:** one per-tick path; aggregated steps replay it; stable sorted traversal. **Skills/MCPs:** implementation worker; no browser/Unity MCP. **Verification:** focused Vitest at maxWorkers=1 including mandatory 100-vs-100. **Report:** standard format plus phase/order evidence. **Stopping rule:** stop before introducing time APIs, variable delta, or unapproved persistence.

## 4. Implement survival, subsystems, alerts, events, and equipment snapshot

- [x] **Objective:** implement deterministic starvation, leak/oxygen, thermal/radiation/contamination/health/incapacitation, canonical alerts/events, and Agent O projection. **Files/search targets:** `src/suit/**`, focused suit survival/equipment tests. **Acceptance:** requirements and tests 12–26 pass; energy/oxygen/contamination bounds hold; radiation monotonic; `SubsystemFault` uses binary `1/1`; direct accepted commands emit canonical alert deltas while aggregate execution emits consolidated phase-11 deltas without duplicates; derived alert IDs/remainder keys remain regex-valid and <=128 characters for maximum-length public IDs; bus projection cannot authorize/mutate. **Guidance:** definition-driven rates/thresholds only; `SuitRecoveryRules` owns per-command limits while state owns the initial health repair ceiling; stable priority/ID tie-break; filtering only incoming contamination; Faulted is not healed. **Skills/MCPs:** implementation worker; no world/combat/resource/UI integration. **Verification:** concrete focused suit Vitest files with maxWorkers=1. **Report:** standard format plus alert/event signature evidence. **Stopping rule:** stop if behavior needs physics, combat, resource, UI, renderer, or random authority.

## 5. Add deterministic browser proof and evidence generator

- [x] **Objective:** prove the pure module on normal `/` and produce timestamp-free evidence. **Files/search targets:** `tests/e2e/suit-survival-state-core.spec.ts`, two named evidence files, `src/suit/**` only if test exposes an in-contract defect. **Acceptance:** pre-navigation collectors, absent `window.TestBridge`, only `/src/suit/index.ts` dynamic import, prescribed scenario twice, correct alert transitions, identical signatures, health 0/0/0/0; evidence regenerates twice byte-identically; no screenshots/group assignment. **Guidance:** do not edit `main.ts`, configs, package/lock, or use TestBridge. **Skills/MCPs:** browser/test runner as authorized; no Unity. **Verification:** `npm run test:e2e -- tests/e2e/suit-survival-state-core.spec.ts --workers=1 --retries=0`, two generation/hash passes, port cleanup. **Report:** standard format plus collector counts/signature/hash. **Stopping rule:** stop if normal `/` requires app or config changes outside allowlist.

## 6. Document contract and run full verification

- [x] **Objective:** publish the named domain document and execute the complete Node 22 protocol. **Files/search targets:** `docs/browser-mainline/suit-survival-state-core-v1.md`, all allowlisted changed paths; forbidden imports/APIs and secrets. **Acceptance:** documentation distinguishes implemented core from non-goals; all protocol commands/scans pass; package/lock unchanged; exact allowlist; port clean. **Guidance:** no opportunistic edits or E2E group assignment. **Skills/MCPs:** test-runner and `verification-before-completion`; orchestrator-managed verification. **Verification:** every command and scan in `tests/test-protocol.md`. **Report:** standard format plus exact command outcomes and evidence hashes. **Stopping rule:** stop on any failure; preserve logs and do not claim completion.

## 7. Independent review and fixes

- [x] **Objective:** obtain correctness/reuse review from reviewer and reviewer-GLM and resolve concrete findings. **Files/search targets:** allowlisted diff and normative artifacts. **Acceptance:** no unresolved critical/major correctness, determinism, authority, duplication, or test-gap finding. **Guidance:** fixes remain allowlisted and are reverified; no scope expansion. **Skills/MCPs:** `requesting-code-review`, reviewer, reviewer-GLM. **Verification:** rerun affected focused tests then full applicable protocol. **Report:** standard format plus findings/dispositions. **Stopping rule:** return to orchestrator if a finding needs forbidden scope or architecture change.

## 8. Completion preflight and final human review

- [x] **Objective:** gate completion after fresh verification. **Files/search targets:** tasks, verification evidence, final allowlisted diff. **Acceptance:** DevToolbox completion preflight succeeds, then exactly one final human review is recorded. **Guidance:** do not toggle before evidence; no repeated human review, PR, merge, archive, or CI grouping. **Skills/MCPs:** `devtoolbox-specs-execution`; orchestrator completion preflight. **Verification:** completion preflight after all prior checks. **Report:** standard format plus preflight and one human decision. **Stopping rule:** stop without completion if preflight or human review fails.

## 9. Authorized publication only

- [ ] **Objective:** create commit `#WELTRAUM-000 Add suit survival state core` and publish branch only after explicit authorization. **Files/search targets:** final exact allowlisted diff; branch `feature/browser-suit-survival-state-core-v1`. **Acceptance:** non-force push succeeds and `git ls-remote` confirms remote branch. **Guidance:** no amend/force; no PR, merge, archive, or CI group. **Skills/MCPs:** `commit-message` if needed; shell only when authorized. **Verification:** status/diff/log precommit, push result, `ls-remote`. **Report:** commit SHA and remote confirmation, no sensitive values. **Stopping rule:** without explicit publication authorization, skip this task and stop after Task 8.
