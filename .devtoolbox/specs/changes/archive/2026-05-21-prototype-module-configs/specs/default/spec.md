# Capability: Prototype Module Configs

## Requirement: Prototype values are configurable

The generated prototype ship SHALL expose important module values through a simple configuration path.

## Requirement: Bootstrap remains playable

The bootstrap-generated default ship SHALL still work without requiring manual asset setup.

## Requirement: No final ship editor architecture

The configuration layer SHALL NOT introduce inventory, crafting, savegame, or final ship editor architecture.
