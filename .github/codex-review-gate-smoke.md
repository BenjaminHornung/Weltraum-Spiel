# Codex review gate smoke test

This temporary pull request exists only to verify that the trusted-base Codex review gate can use the configured `CODEX_REVIEW_TOKEN`, request review for the exact current head, recognize the live Codex `Review Result` format, and publish the custom commit status.

Second attempt after merging the live result-comment compatibility fix.
