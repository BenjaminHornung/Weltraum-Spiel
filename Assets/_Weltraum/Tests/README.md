# Test Strategy

This folder holds skeleton verification for the Clean-Core runtime slice.

- EditMode tests are compile/discovery smoke tests for the assembly layout.
- PlayMode tests will be added later for runtime behavior.
- Fixtures are reserved for future shared test data.
