# Scene Rules

No scenes are created in this slice.

Purpose of the scene folders:
- `Product`: shipped gameplay scenes later
- `VerticalSlices`: focused feature demos
- `TestRanges`: validation and sandbox scenes
- `UIShowroom`: UI-only showcase scenes

Future scenes need manifests, validation, and screenshot evidence. Scene files must remain composition-only and contain no business logic.
