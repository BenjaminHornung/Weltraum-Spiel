# P07 — end-to-end evidence and remaining measurement boundaries

## Goal and current scope

Produce an evidence-bound report for actual input-to-Applied timing, actual first committed render submission, separate cold/warm scenario populations and all outcomes. Preserve the original250ms p95 target and distinguish the proposed render/hold/main-slice targets. No quantile addition, success-only selection, fabricated timestamps or silent acceptance of missing samples.

The original P07 write scope was the performance reporter, its unit tests, the performance runner, the designated E2E file and this report. The Owner subsequently authorized bounded body main-thread observation and a narrow Restore-/Recut render-identity fix; those permissions do not authorize a new physics or worker contract. A later explicit request authorized a GitHub branch snapshot, not a main merge or a release. P00-D's new detailed worker spans remain explicitly deferred.

## Reuse and observed gaps

- Existing `tests/e2e/hvp-performance-evidence.ts:51–54` already exports the required finite/nonnegative, copy-sorting nearest-rank function. Reuse it rather than duplicate a statistics library. The existing frame/lifecycle assessment is not the new Cut RT scenario contract.
- Existing `hvp-performance.spec.ts` demonstrates source/build/lock/WASM/browser binding, headed browser process identity, actual selected GPU, foreground/canvas/DPR checks, bounded PerformanceObserver capture including `PerformanceMeasure.detail`, and preservation of failed populations. Its implemented workloads are startup, walking, lifecycle and neighbor crossings—not body-cut latency.
- Main terrain commands already emit command-bound Input/Applied/committed-render observations through P00. The current `bodyCutConsumer.ts:25–63` does not emit equivalent observations: it records its immutable Applied outcome only after actual native finalize/render finish, without a timestamp. The current committed-render observer checks main terrain Root/keys, not body-child membership. A later UI polling timestamp or a main-terrain render match must not be presented as exact body timing/render proof.
- Normal body recuts are functional: the fresh P05 production run passes terrain/timber recuts and both cold recut-restoration/next-cut cases. This does not fill the missing timing boundary.
- Source/device bindings will describe the real HEAD plus uncommitted overlay and production build, never a fabricated candidate commit. I01 is already combined sequentially in this task-owned worktree; no duplicate P03 adoption or Main merge is needed.

## Reporter and run preparation

1. Keep the package's `HvpCutSample` fields and five outcomes. Group by scenario and temperature; count every supplied attempt. Missing nullable values remain missing, invalid/negative/nonfinite timing is reported rather than clamped. Descriptive quantiles do not by themselves grant hardware/sample-count/functional acceptance.
2. Reuse nearestRank and add the specified1..100/1..15/immutability/invalid-value tests plus incomplete Applied and Timeout accounting. Preserve raw observations separately from summaries.
3. Resolve the body observation scope before collecting a known-incomplete large body population. A bounded product-observation extension requires explicit Owner approval; it must preserve gameplay, source/physics/save contracts, count controlled payload and leave the deferred worker transport unimplemented.
4. Freeze the reference device, actual browser/GPU/power/profile, source/build/fixture hashes and a fixed attempt/session schedule before measurement. At least100 regular attempts per primary class/temperature across at least three fresh sessions are planned; fewer than100 valid measurements remain NOT_PROVEN. Failed/precondition attempts remain in the raw series and cannot be replaced until enough successes happen.
5. Run pilot functional observations first, then the fixed quiet production series without tracing/screenshots/video. Visual captures and native fault/recovery checks remain separately classified. P06/X01 are conditional on actual remaining costs, not the P03 synthetic whole-job timing or old historical hold numbers.

## Device observations and the later Owner designation

Current read-only host observations on2026-09-22: Windows11 Enterprise10.0.26200 x64; Intel Core Ultra7 255H,16 reported cores/logical processors;33,811,132,416 RAM bytes. Enumerated adapters: NVIDIA RTX PRO500 Blackwell Generation Laptop GPU driver32.0.15.9658 and Intel Arc Pro140T driver32.0.101.8517. The Intel-connected display reports1920 horizontal pixels/60Hz. Balanced is the active power scheme. Enumeration is not proof of the browser's selected GPU or physical VRAM. The existing neutral HVP-DEVICE-01 precedent uses1280x720/DPR1 and explicitly did not grant target-device acceptance; actual browser selection/power/foreground must be verified freshly.

## Status

- [x] P05 full combined baseline completed (205/205 files, 2271/2271 tests); the local combined candidate also passed 206/206 files and 2343/2343 tests on its fresh unchanged rerun. No candidate commit SHA was fabricated.
- [x] Owner approved the bounded additional body main-thread measurement scope.
- [x] Owner designated this local device for the formal250ms comparison at1280x720/DPR1/Balanced, subject to fresh actual browser/GPU/power binding.
- [x] Reporter and deterministic unit checks:38 focused reporter/extractor tests PASS; invalid and incomplete populations remain explicit.
- [x] Body-observation pilot and a predeclared 14-case Cold/Warm diagnostic matrix; neither supplies the formal ≥100-valid-per-class population or full fault matrix.
- [ ] Final measured populations and separate CODE/FUNCTIONAL/NATIVE/BROWSER/PERFORMANCE/VISUAL_OWNER report.

No P07 performance, complete fault matrix or visual Owner acceptance is claimed at this preparation checkpoint.

## Bound main-thread body observation implementation

The fresh P05 combined baseline completed before these changes:205 files/2271 tests PASS, zero skipped, Exit0 (`p05-full-unit-self-verified.json`,2026-09-22T17:32:56.671Z). Existing worker, native, source and save files remain protected.

1. Add an optional final trace argument and one-way `disableTrace()` to the existing body consumer. Emit distinct main-thread `cutBodySubmittedMs` and `cutBodyTotalApplied/Rejected/RecoveryHoldMs` at the actual accepted/rejected command and terminal assignment. Replay/conflict retain original promise/error behavior; diagnostics cannot replace outcomes, add awaits or persist fields. No worker timings or fabricated body-hold value.
2. Extend the existing cut observer, not a second observer implementation. Body phases reuse the real confirm timestamp, bounded input records, single pending candidate, finite-clock validation, reentrancy guards and actual render result handling. Terrain observations remain unchanged. A body pending candidate additionally holds only a WeakRef to the actual immutable consumer outcome, the native moving sequence/parent ID, at most32 child owner/source/render bindings and bounded active/visible body-key sets. Restore/new outcomes, mismatched native receipts, stale children or renderer failure never produce success.
3. Bootstrap supplies facts from one actual native snapshot plus the existing fragment/branch entries and direct representation children. Require native moving Idle/Applied receipt with the same command, absent retired parent, exact children and matching native source digests; child geometry keys and float32-projected poses must match actual rendered nodes. Exact active/visible body-key equality detects stale parent/child projections. Preserve the real terrain Root identity/revision too: body recuts do not increment it. The report's source-generation fields remain actual terrain Root generations, with body sequence and child source bindings explicitly separate; never pretend a new structural child retains an edited parent's revision.
4. Retain one observer with a conservative512KiB controlled reservation, inside the unchanged256MiB gameplay cap. Main/body pre/post/pending bounded key sets plus at most three32-child witness lists and fixed records fit this modeled allowance. All collection is bounded before growth, no source/mesh buffers or native snapshots are retained, and revocation disables both producers. Existing main-thread synchronous release policy applies; no deferred worker packet is introduced.
5. Add focused consumer/observer/bootstrap tests first: actual input ordering and terminal timing, original promises/errors/receipts, disable/dispose, body-vs-terrain attribution, live/empty child sets, mismatched native/source/pose/visibility, stale restore/outcomes, malformed facts, unavailable/throwing renderer and exactly-once accepted submit. Existing terrain/C2B cases remain active; their exact-cap fixture must still exceed the larger optional reserve without changing its assertions. Then run fresh functional body observations before any large performance series.

## Explicit Owner decisions

The Owner selected **Mainthread-Messung ergänzen (Recommended)**: add exact body input/Applied and correctly attributed committed-body-render observation in the body consumer, bootstrap/trace observer and focused tests. This is a narrow extension of the previous test-only P07 scope. Physics, gameplay behavior, source/save formats and caps remain unchanged; controlled observation payload must be bounded/accounted and the previously deferred detailed worker spans remain absent. Freeze the concrete source/receipt/render binding before implementation and run focused failure/identity/cancellation tests before collecting a large population.

The Owner also selected **Dieses Gerät (Recommended)** as the reference device. Current Windows/Core Ultra7 host observations may anchor that designation, but adapter enumeration still does not establish the selected WebGL renderer. Browser/GPU/driver/power, exact1280x720/DPR1 foreground canvas and source/build bindings must be freshly observed before the fixed series. No OS power or display setting change is authorized or needed by this decision.

## Main-thread extension implemented and checked

The existing consumer now emits only optional, guarded main-thread submission/terminal spans and exposes one-way trace disablement. Native/compile/stage/finalize ordering, original promises, errors and saved receipts are unchanged. The existing observer handles body/terrain inputs in separate bounded ID namespaces, but still owns only one pending candidate. Body confirmation additionally compares the stable consumer-outcome WeakRef, actual native sequence/receipt, copied child source/render bindings and exact active/visible body sets. Bootstrap checks real admitted child artifacts and float32 native-to-Three poses. Complete removal remains valid with no invented child; restore/recovery/changed facts never count as a successful render.

The512KiB controlled observer allowance is charged through the unchanged admission-first reserve policy. Five simultaneous main and five body key lists, at most three32-child binding lists and fixed records fit this logical bound. No full source, mesh buffer or native snapshot is retained. The existing exact-cap preview test uses512 bounded preview cells so it still crosses the larger optional reserve; original cap/error/rollback assertions remain intact and both producers are proven disabled on revocation.

Test-first evidence retains four missing-consumer-marker failures and21 missing-body-observer failures. Small subsequent groups passed20 consumer cases and64 observer cases. The new bootstrap test uses actual staging and the real Three backend/projection with an explicitly fake transport; it tests source/pose/receipt mismatch and stale-parent rejection, not native-cut success. Its one fixture typing error was corrected with an explicit factory-presence guard. Fresh combined verification then passed4 files/148 tests, zero skipped, actual Exit0 (`p07-mainthread-focused-self-verified.json`,263.74s). TypeScript/diff-check and production build PASS,272 modules; existing chunk warning only.

Raw blobs: `cutTrace.ts` b3da9ed8cdfa2a7edd60a5419ec0fae761e9b5af; `bodyCutConsumer.ts` 7fd89b74beecfb4bf78ac4d0bd4fff87d743fa30; `hvpBootstrap.ts` e2c0b1e4cf1ce43a7f15ff86554271cc116520ad; tests `hvpCutTrace.test.ts` 2629015e0958ad04797c852cb013dbe9b01540f2, `hvp-moving-consumer.test.ts` 49baab118c5ca961d1668b06a90e683df0e49cdf, `hvp-bootstrap.test.ts` 5e9ae4f973251fc944851764b9494c8963d4107e. Physics/client/worker, compiler and generic pool were not changed by this extension.

## Real body pilot — functional proof, not formal p95

On the production bundle `index-D5_2orCQ.js`, actual normal pointer/keyboard input detached the384-cell rock arm and then recut it with Box and Sphere. Both body commands were Applied/Idle, removed their actual parent, preserved total material cells/mass with the removal receipt, and produced exactly one matching input/Applied and committed-body-render observation. Child digests/render keys and native sequences1/2 match; the real terrain Root stays at generation1. Final measurement health and the bounded collector report zero errors/drops and no pending input/render. Body-hold duration remains null/NOT_MEASURED, not the unrelated old terrain hold.

Local raw artifact `p07-body-mainthread-pilot-20260922-01.json` retains all snapshots, receipts, observations and precondition notes. Box input-to-Applied3046.900ms/render3085.200ms; Sphere3695.200ms/render3731.700ms. The preceding terrain detach took2024.800ms to Applied. These individual values are well above250ms; they are not a formal p95 or an accepted target-device population. This interactive pilot used1536x791/DPR1.25, not the designated formal viewport. Existing clock evidence records StageBodyCut handlers1446/1637ms and timer gaps1470.4/1647.8ms, plus an intervening historical78.5ms Read-associated SimulationHold. Handler duration is not relabelled as exact World-Hold or codec cost.

The pilot first referenced a control hidden during Play, before any cut. Before Sphere, Pointer Lock was absent and the real pause dialog intercepted the background Play button; the actual Resume control restored the test precondition. No cut was repeated, discarded or selected for speed. The managed preview reached its explicit15-minute lifetime while the already-loaded SPA remained available; it was not reported as an Exit0 server run or restarted. All actual cut/marker checks completed, the owned browser tab was closed, the original blank tab preserved and port5177 has no listener. The raw capture was moved from the browser tool's allowed output root to the local evidence directory; no upload or OS capture occurred.

Final combined regression/bindings and the fixed formal populations remain pending. This pilot does not grant visual Owner acceptance or automatically authorize a new physics/cap change.

## Reporter, exact timeout input and reference-device runner

The descriptive reporter preserves all five outcomes and malformed/incomplete rows, reuses the existing nearest-rank helper and never grants sample/device acceptance from descriptive quantiles. Its command-scoped marker extractor rejects duplicate/misordered/missing exact input instead of using the automation clock. Fresh reporter/extractor tests pass38/38. Planning unresolved/timeout attempts also exposed that the actual confirm time was emitted only with a terminal; a bounded ten-line observer addition now emits `cutInputMs`/`cutBodyInputMs` at actual ID binding using the already captured confirm timestamp, with no new clock or retention. Four genuine regression failures preceded the fix; the related trace/terrain/body/reporter group passes132/132 and TypeScript passes. The earlier body-pilot hashes remain historical.

The new runner uses normal-UI checkpoints, new documents/workers per attempt, and one unmeasured cut plus normal hot restore for Warm. It records all precondition failures separately when no honest command/input exists. Seven variants separate quarry Box/Sphere and body Box/Sphere moving/sleeping; moving uses a real contact impulse and is explicitly not a falling-body substitute. Diagnostic discovery lists14 one-attempt cases; the proposed formal schedule is34/33/33 per variant/temperature across three fresh browser processes. No formal series has started or been accepted.

The source/build/fixture/configuration, lock/WASM/browser binary, actual browser PID/start, GPU/driver, Balanced power source and exact foreground1280x720/DPR1 canvas are bound before inputs and checked afterward. Raw samples, full snapshots and every warm-up/failure are retained; body-hold stays null. The task-local external configuration reuses the existing performance configuration with the pinned preview command and disables traces/screenshots/video/retries. Latest TypeScript, diff-check and272-module production build pass (`index-BVeq1EXN.js`, existing chunk warning).

### Two-case binding pilot: real Hot-Load/recut blocker

Predeclared diagnostic probes were quarry-Box Cold and body-Box sleeping Warm, one attempt each; `p07-binding-pilot-01` completed with1 PASS/1 FAIL, actual Exit1, zero retries. Both cases freshly identified the selected Intel Arc Pro140T device32902/32081, driver32.0.101.8517, exact1280x720 framebuffer/DPR1 and foreground state. The cold quarry case completed. In the Warm body case, an actual warm-up cut and normal hot restore succeeded, then the measured `moving-cut-1` was Rejected with `HVP terrain sector upsert failed: RepresentationRemovedAtRevision`. Its input timestamp and real rejection remain in `attempt-1.json`; no success timestamp was invented. The owned browser/preview finished cleanup and port5173 has no listener.

Source diagnosis: hot restore uses `:load${restoreSequence}` aliases for restored terrain/fragment/branch artifacts and retires old artifacts. Subsequent body staging again derives the old child key and source/artifact generation from the restored native sequence. The generic backend correctly refuses an equal-version key whose prior ledger state is Removed. Main terrain and branch factories share the relevant generation/key pattern. Owner explicitly approved a narrow Restore-/Recut render-identity/restore-wiring repair; canonical IDs, save, physics, admission caps and the backend tombstone guard remain protected. The failed pilot is retained, not reclassified.

### Restore-/Recut render candidate — bounded diagnostic gate

Only scene-key derivation for hidden terrain/body/structural stages changed: one monotonic render-only `:stageN` suffix per stage invocation, including failed/rolled-back candidates. Existing rollback/finish, current native owners, source revisions, persistent IDs and resource caps remain. The bootstrap regression first reproduced the real Three backend's `RepresentationRemovedAtRevision` on a second same-revision terrain stage (`p07-restore-key-valid-red.json`), then demonstrated terrain, branch and body rollback/retry, body source/pose/visible-key proofs and original observation contract (`p07-restore-key-first-green.json`). The initial separate Red report failed earlier on a synthetic sector's same-version content conflict and was corrected in the fixture, not the backend. Fresh six-file focused check: 172/172 PASS, TypeScript noEmit, Vite production build 272 modules and diff-check PASS (`p07-restore-targeted-units.json`).

The original Warm body pilot remained failed at `p07-binding-pilot-01`. Two subsequent independent diagnostic starts were retained as precondition failures before measured input (`p07-restore-warm-pilot-01`, `p07-restore-warm-diagnostic-02`); neither is selected as a latency sample or a success. A fresh bounded diagnostic `p07-play-precondition-diagnostic-03` completed one normal Warm body recut on the reference device: actual native Applied, exactly one body input and bound committed-render marker, child key `hvp:terrain-fragment:r1:1b4aa69d:r1:p0:stage2`, raw input 22753.300 ms, Applied 25233.200 ms, committed RenderSubmit 25257.000 ms (durations approximately 2480/2504 ms); separate real warm-up records retained. Body hold remains null. This demonstrates the former tombstone rejection did not recur in that diagnostic attempt; it is **not** a formal multi-session p95 pass. Two independent technical reviews are pending at this gate, so the repair is not yet declared complete. No R00 Core extraction or new dependency was added. Local planner handoff: external `R00-SOURCE-LEASE-GATE-2026-09-22.md`; local overlay `UNCOMMITTED_NOT_TRANSFERRED`.

The first two read-only technical reviews found one material follow-up: `representationKey` permits only 128 chars, so a valid 127-char native owner plus `:stageN` was newly invalid (genuine unit Red `p07-restore-long-id-red.json`). Body staging now uses short scene-only `hvp:fragment:stageN:pI` keys. A further independent review found that a valid cold native owner can share this syntax; the local stage checks native bodies/parked/structural owners, existing fragments/branches, candidate owners and historical render keys before choosing a monotonically fresh alias. Both affected read-only reviews now find no confirmed blocking defect for that body path. `managedFragmentKeys` stays add-only intentionally: reversible eviction rehydrates the same artifact and the history gates stale visibility. The 127-char-owner and colliding-native-owner unit checks pass; rollback preserves resident/visible sets and the original ledger. A complete cold-restored-colliding-resident fixture remains unproven. Crafted but schema-valid saved IDs colliding with terrain/foliage stage aliases were separately noted as low residual risks; no blanket arbitrary-save acceptance is claimed. New current warm diagnostic `p07-restore-collision-warm-05` passed one real hot-restore/recut with alias `hvp:fragment:stage2:p0`, native Applied and a bound render, source/build inventory unchanged; input 26454.400 ms, Applied 28988.300 ms, RenderSubmit 29048.700 ms (~2534/2594 ms), body hold null. Prior failed attempts and the earlier successful pre-guard diagnostic remain in separate artifacts. TypeScript, final 272-module production build and six-file/172-unit regression all PASS on this candidate (`p07-restore-collision-final-units.json`). Cold terrain/branch and live body/foliage recuts pass 4/4 normal production-browser E2Es (`p07-restore-final-browser-01`), port5177 is free. The first combined full-unit regression `p07-combined-full-unit.json` finished Exit1: 205/206 files and 2342/2343 tests PASS; the unchanged `hvp-branch-session.test.ts` canonical timber/Rapier case hit Vitest's existing 5-second test limit. The same existing file then passed 3/3 under the unchanged default limit in isolation (`p07-branch-timeout-isolated.json`, timber case 2864 ms). No physics/test timeout or product code was changed; a second full run `p07-combined-full-unit-rerun.json` on the unchanged candidate completed Exit0, 206/206 files and 2343/2343 tests PASS in 1047.36 s. The first suite remains a retained FAIL, not retroactively called PASS. The original ≤250-ms p95 target is **NOT_PROVEN**.

### P07 first complete diagnostic matrix — performance gate remains open

The predeclared local fourteen-case diagnostic matrix (`E/P07-DIAGNOSTIC-MATRIX-01.md`, raw `E/p07-diagnostic-matrix-01/`) passed 14/14 functional Playwright cases, one real Applied cut in each of seven variants × Cold/Warm, no retries/skips, identical source/build inventories, task-owned preview cleaned. For individual samples only: quarry Box 272.3/246.6 ms, quarry Sphere 313.4/231.3 ms; rock arm 1870.9/2086.1 ms; body Box moving 2924.6/2628.9 ms, sleeping 2983.6/3510.1 ms; body Sphere moving 3658.7/3358.1 ms, sleeping 3286.4/3102.3 ms (Cold/Warm input→Applied). RenderSubmit is later in all cases, and body hold is unknown/null. These are n=1 per cell, **not** p95 measurements. Rock-arm Cold includes 1283.1-ms support analysis, native recipe230.0-ms, true hold357.7-ms and timer gap236.0-ms; the P06 prerequisite of no recipe-caused timer gap >20 ms is not met. Do not start P06 merely from this slow body/terrain observation or declare the original250ms contract failed/passed on a tiny population. The formal100×class×temperature/three-session performance acceptance, broader fault/recovery matrix and Visual Owner review are still open; additional cooperative recipe/quiescence work would need an explicit revised scope/contract rather than a silent cap, engine, physics or worker-transport change.

The later Owner-requested branch publication includes the [predeclared matrix](P07-DIAGNOSTIC-MATRIX-01.md), the [selected original JSON reports with per-file SHA-256/redaction records](../../../apps/weltraum-browser/evidence/hvp-cut-rt-v2/README.md), and the [fixed R00 handoff](R00-SOURCE-LEASE-GATE-2026-09-22.md). The earlier `E/` paths above identify the original local artifacts; reports retain their pre-commit source/build bindings and failures. No subsequent publication changes the diagnostic-only, incomplete-performance status.
