# Cut RT — prospective cooperative follow-up (plan only)

**Status:** Reviewed, read-only proposal; not an activated P06 card or a product write lease. Prepared after the Owner selected *Folgeplanung erstellen*. The working branch is `feature/hvp-cut-rt-p00`, based on `9341fb906383515057e659a99e16a381632f2bea`. See [the source/lease extract](R00-SOURCE-LEASE-GATE-2026-09-22.md) for snapshot provenance. The independent R00 planner may use a fixed snapshot; this Hestia plan does not depend on R00 and does not create a reusable game engine.

## Goal, observed constraints, stop line

Preserve exact existing 0.125-m source/structural/mesh/native/persistence behavior while investigating how to reach the **original real Input→Applied p95 ≤ 250 ms**, without transferring a multi-second synchronous stall to another part of the physics worker. Do not loosen the 256 MiB CPU, 128 MiB mesh, 500k-triangle, 300-draw, 96 MiB prepare, two-heavy-compiler-job/queue-32 or body/fragment limits, timeouts or validation tolerances. `RecoveryHold`, actual rollback, native current-pose binding and identical save bytes remain mandatory. Do not introduce an Hestia import into an existing pure `src/voxel/**` algorithm, a generic job/wire abstraction, new dependency or render-layer World authority.

Evidence: the unchanged full-unit rerun passed 206 files/2343 tests; the initial run's unrelated five-second Rapier-test timeout is retained separately. The 14/14 real Cold/Warm diagnostic cases each have **one** Applied cut per variant/temperature, not a p95 population. Quarry Input→Applied took 231–313 ms, rock arm 1871–2086 ms and body cuts 2629–3659 ms. Rock-arm Cold included SupportAnalyze 1283.1 ms, Compile 177.9 ms, NativePrepare 242.5 ms, recipe 230 ms, true Hold 357.7 ms, and a `PrepareTerrain` handler/timer gap of 231.9/236 ms. Body Sphere moving Cold had a `StageBodyCut` handler/timer gap of 1631.8/1645.9 ms; **body Hold is not measured**. Do not infer subphase cost by subtracting overlapping spans. P00-D detailed worker spans remain deferred. The subsequent Owner-requested GitHub snapshot includes [selected original JSON reports and their redaction manifest](../../../apps/weltraum-browser/evidence/hvp-cut-rt-v2/README.md); the unredacted local originals remain outside the repository.

The existing P06 package requires both demonstrated Hold/fragment-render cost and **no recipe-caused Physics timer gap > 20 ms** before moving a synchronous recipe ahead of the pause. The latter prerequisite fails on the observed `PrepareTerrain` callback. P06 excludes moving-body recuts. X01 is not selected by the rock-arm cook time of 0.7 ms. Do not start P06 verbatim or a formal 100-per-class series on the known candidate.

## Ordered prospective slices — no implementation authorization implied

### 1. Attribute real latency without claiming new worker spans

Inspect `tests/performance/hvp-cut-rt.spec.ts`, `tests/performance/hvpCutRtReport.ts`, `src/hestia-prototype/physics/{physicsWorker,session,bodyCutSession}.ts`, `src/hestia-prototype/terrain/terrainProducts.ts` and `src/workers/hvpBodyCutJob.ts`. Re-fingerprint the actual branch/source/build/fixtures/browser/power profile first. Compare retained clock `delayedCallbacks.previousCommand/handlerMs` and main markers for matching scenario, motion and temperature. A bounded diagnostic-only independent job/stage fixture may compare a real-size body candidate using actual source, one worker and zero retries, without fabricating native success or changing Goldens. Record failures and unknown ownership. If attribution requires P00-D worker spans, **stop for an explicit contract and byte-budget decision**; do not add them silently. Confirm recipe causation of the terrain gap on the bound host before designing slice 2. Elapsed asynchronous handler time is not synchronous CPU time; a previous terrain Hold is not body Hold.

### 2. Design the cooperative Terrain recipe prerequisite separately

**Possible owning files after separate approval:** `src/hestia-prototype/physics/{terrainFragment,rigidRecipe,session,physicsWorker,client}.ts`, `src/hestia-prototype/terrain/terrainConsumer.ts` and adjacent physics/clock/transferred-recipe tests. Pure `src/voxel/adaptive/**` and `src/voxel/structural/**` remain read-only absent a separately proven engine-neutral algorithm contract. Retain existing transferred exact mass, connectivity, coverage, canonical ordered-collider admission and a single native-issued recipe; no replicated worker authority or loosened tolerance.

Design owner-local, genuinely yielding preparation (not `Promise.resolve`) with explicit source/session/epoch/command/generation identity, cancellation/dispose cleanup and coexistence accounting. **Recipe preparation itself is non-mutating; normal Read/Input and simulation continue**, while competing mutating transactions and Saves are blocked. Define Pause/Inspect, cancellation and Dispose explicitly. Recheck currentness after each await; an older SourceReady reply/snapshot must not overwrite a newer Read pose. Pause the real World only for present capped staging and paired publication, binding current native pose/velocity there. The present delayed-callback list logs gaps only above 60 ms; absence cannot prove the 20-ms prerequisite.

After approval, reuse existing transferred-recipe, clock, native and save assertions; add only new behavior cases. On an actual 384-cell fragment, prove no recipe-caused gap > 20 ms and target slices ≤ 4 ms without moving the old 230-ms stall outside the pause. Check ordered colliders, mass/COM/inertia, source/epoch, neighbor exclusion, cancel/late/dispose, rollback/Finalize/sticky RecoveryHold and complete save bytes at *matched deterministic simulation states*. If these invariants cannot hold, **stop** rather than relabel the old P06 design.

### 3. Investigate moving-body local work and native staging independently

**Possible owning files after separate approval:** `src/hestia-prototype/physics/{bodyCutPlan,bodyCut,bodyCutSession,structuralPlan,physicsWorker,client}.ts`, `src/workers/hvpBodyCutJob.ts`, `src/hestia-prototype/terrain/{terrainProducts,bodyCutConsumer}.ts` and focused moving-session/wire/pool/consumer tests. This is Hestia session/adapter work, not a pure-core generalization. Actual native staging still derives a plan inside the World; the V2 local worker independently derives and self-decodes products. The real 1631.8-ms `StageBodyCut` handler is a stall, **not** an attributed set of subphases. Choose the smallest evidenced change only after slice 1.

Any pre-stage native-issued source plan must bind the *originally issued hit*, immutable source, parent membership, products, request/epoch/revision and single physics owner. Do not perform a new stage-time aim/range raycast. Sample the **current** native pose, velocity and momentum at staging; do not freeze input-time pose or promote an independently computed worker product to native-issued authority. Persistent receipts/schema remain unchanged absent separate approval. Reuse existing cases and add only new box/sphere, moving/sleeping, mixed-material, full/partial, stale/cancel/late, restore/fault/rollback/finalize and actual Rapier mass/current-pose tests. Budget simultaneously retained input, local/native output and old/new scene. A shorter named Hold with a longer worker stall is not a win.

### 4. Re-evaluate P06 and P07 only when eligible

After the cooperative recipe prerequisite and body path are independently established, re-evaluate P06's **other** entry condition with fresh World-Hold/fragment-render evidence. If still warranted, seek separate approval for its source-admission contract; it cannot replace body optimization. P07 must then freeze fresh source/build/device bindings, retain *all* attempts and failures, and require ≥100 valid regular cuts per primary scenario/temperature across ≥3 fresh sessions, with nearest-rank p50/p95/p99/max and separate CODE/FUNCTIONAL/NATIVE/BROWSER/PERFORMANCE/VISUAL_OWNER statuses. The existing 14-case diagnostic series cannot substitute for this acceptance population.

## Decision and review boundary

The Owner approved **planning only**, not new worker/native transaction or body protocols. Before a slice-2/3 write, provide the precise request/reply, pause/currentness/cancellation semantics, limits, negative cases, file boundaries and source-byte binding for approval. Even a cheaper out-of-package change needs its own scope authorization. Independent read-only `reviewer` and `glm-plan` reviewed the original prospective plan; their factual and wording corrections are incorporated here. Their review did **not** run new tests or grant implementation authorization. R00 may advise separately but is not an activation gate.
