# P04 — synchronous presentation batches and shadow revisions

## Goal and constraints

Coalesce HVP projection/visibility publication within an already synchronous resource batch. Cache shadows from actual visible casters and shadow lights, not receiver-only changes. Preserve active-key transitions, native/root/render publication order, cleanup, all gameplay caps, geometry, materials, lighting, AO and shadow-map settings. No microtask/async publication, generic-backend refactor or source-authority shortcut.

Execution remains direct and serial in the task-owned worktree. P03's full regression must finish before source/test writes begin. No agent, Plannotator, commit or publication. The P00-D worker-span deferral remains in force.

## Current source evidence

- `hvp/hvpBootstrap.ts`, raw `0c04e9f78b36e1cccc9c44770700717b145f4b04`: adapter435–529 publishes after each accepted upsert/remove; `stageTerrain`1664–1711 and moving-body staging1733–1776 contain the intended synchronous upload/retire loops. Their explicit publish/restore calls stay outside the resource-loop batches and retain their existing order.
- `presentation/visualEffects.ts`, raw `0c44da6c1041c00c66c4cbf2a17f6b383febc3a9`: current prior-map retains every Mesh/geometry/pose, including noncasters. The existing exclusions for water/coast/tool/salvage remain unchanged.
- Protected generic backend raw `fd89c1b74dd9d6b290949472eaa48199d37980df`: newly registered artifacts remain hidden without an admitted projection/visibility entry (`recomputeVisibility`390–409). Batching does not bypass artifact checks or generic O(n) registry work.
- Protected material factory raw `d01d01acab9dd669199561e3d6a3c1ce1abd7283` produces ordinary MeshBasic/MeshLambert materials; the HVP mesh factory produces ordinary Meshes. HVP uses one PCF shadow-casting DirectionalLight.

Context7 lookup failed with `Invalid API key`; credentials/configuration were not inspected or changed and the request was not retried. Installed pinned Three.js source/types are the API evidence: WebGLShadowMap509–567 respects parent visibility, main-camera layers, material visibility/groups, and actual caster state;432–494 reads shadowSide, wireframe, alpha/texture and clipping/deformation settings. BufferAttribute has a real id/version; BufferGeometry has no usable version field. Derived shadow-camera world matrices are updated during the shadow pass; use their authoritative light/target/up inputs plus projection instead of retaining a stale pre-render derived matrix that would force a second unchanged-frame update.

## Narrow implementation plan

1. Add `presentation/synchronousBatch.ts` plus `tests/unit/hvpPresentationBatch.test.ts`. Verify nested/empty batches, outside requests, value/error identity, combined action/flush failures, recovery of depth/dirty state and rejection of a non-running thenable fixture.
2. Add `batch` to the existing HVP adapter and route its current publication requests through the helper. Wrap only terrain and moving-body generation upload/lease/AO and cleanup loops. Do not batch across awaits, explicit root/native publication or updateTerrainState. Preserve the original action error if cleanup or its flush also fails, retaining both causes. Test the actual adapter with the existing real Three backend/test-renderer pattern for1/8/32 artifacts, hidden staging, partial failure, cleanup and subsequent use.
3. Add the primitive revision comparator in `presentation/shadowRevision.ts`; collect copied tuples in the existing visual renderer. Include effective parent visibility/layers, stable caster IDs, geometry identity, relevant attribute id/version, draw range/groups, copied matrix, material/depth identity and relevant alpha/side/wireframe/clipping state; light/target/projection/up/map-size/bias state. Unknown deformation/custom shadow behavior stays conservatively dirty and is identified in diagnostics. No vertex hashing or retained Mesh/geometry references.
4. Bound cached primitive revision payload per snapshot; account old/new coexistence in `HVP_EFFECT_COST.cpuBytes` without increasing a gameplay cap. Overflow/unsupported data disables only reuse and requests a real shadow redraw, never truncates a valid-looking revision or suppresses rendering. Keep snapshots only after successful rendering, clear on dispose, and preserve an externally pending needsUpdate request. Separate scene-preparation/revision work from render-submit duration; these diagnostics are not GPU timings.
5. Add actual Three-object/visual-renderer tests in `tests/unit/hvpShadowRevision.test.ts`, then self-review and fresh focused/type/build/package checks. Capture static/cut/falling/sleeping evidence using existing browser flows with no new Goldens; report unavailable recovery/visual-owner proof explicitly.

One necessary existing-test adjustment is identified in advance: `hvp-visual-effects.test.ts` (raw `7c90d95aaf70d7c0e68af82f5f66534aa07ace36`) currently expects another shadow redraw when removing a mesh that was already hidden in the previous frame. The new effective-visible-set contract deliberately removes that redundant update. Only that final expected flag will become false, backed by new separate tests that still require updates on hiding/removing a previously visible caster. This tightens the intended optimization assertion; it does not remove a failed correctness test or alter a Golden. All other existing assertions remain protected.

## Verification status

- [x] P03 full baseline completed:202 files/2191 tests PASS, Exit0; P04 test-first preparation started.
- [x] Pure batch helper and actual adapter integration:15 focused tests PASS.
- [x] Shadow comparator/collector, bounded ownership and negative cases implemented and self-reviewed; corrected default-scene reproduction passes.
- [x] Self-review, fresh focused/type/build/package regression:204 files/2246 tests PASS, Exit0.
- [x] Corrected-candidate browser evidence and remaining acceptance limits reported separately.

## Before-run failure and authorized test correction

The unchanged P03 production build passed quarry Box and rock-arm detachment, but the live dormancy case failed its original30s restore assertion (`p04-before-01/report.json`). No P04 product code existed. Read-only analysis of the retained trace found355 parked-body snapshots; all97 snapshots inside the12m wake radius still had `neighbor.busy:true`. The player kept walking, and when neighbor work ended the distance was13.91m. No restore had started; native state stayed Running/Idle, with no dormancy error or RecoveryHold. The existing controller intentionally defers residency during neighbor work.

The Owner explicitly approved stabilizing this test's real input sequence, retaining the original failure and all source/native/restore assertions. The shared live/cold path now releases W inside10m (or once a real body has already returned), then performs the unchanged restore checks while the player remains near. No teleport, product guard, radius, cap, old timeout or assertion was relaxed. Two local screenshots at already asserted sleeping/restored states supply the requested before/after visual evidence; no Golden changes. This adds only the explicitly authorized `tests/e2e/hvp-visible-coast.spec.ts` to the test scope. Its pre-change raw hash was `5e59383df830df35d6125d351d71648b54db7819`. Fresh live/cold rerun on the unchanged P03 production code passed2/2, Exit0, one worker/zero retries (`p04-dormancy-input-verified-01/report.json`). The initial failure remains retained, not reclassified as a pass.

## Implementation and self-review checkpoint

The local synchronous batch owns only depth/dirty scalars. It retains original action/flush causes, including `throw undefined`, and does not start promises. Actual Three-backend adapter tests verify one projection/visibility pair for1/8/32 hidden upserts, failure cleanup and reuse; original generic artifact validation remains active. Only terrain and moving-body staging resource loops are batched; explicit publication and rollback-state restoration keep their original order.

The shadow collector retains only copied primitive tuples. Each snapshot is now bounded to256KiB of modeled payload, including temporary light tuples; old/new coexistence is charged as512KiB in the existing effect ledger. The initial64KiB limit was corrected after the real default-scene reproduction below, not hidden as a successful optimization. No gameplay cap is raised. Unsupported classes/deformation, custom shadow/scene hooks, duplicate IDs and overflow force redraw rather than a partial cache. Geometry/attribute identities and versions, effective visibility/layers, material alpha/texture/clipping state, and light/target/projection state are tracked. Texture transform inputs are captured instead of stale pre-render derived matrices. Standard Three type flags reject unused instancing/video classes without importing their runtime implementations. The cache is admitted only after a successful render and cleared with the scene reference on disposal. CPU preparation and render-submit timings are separate; neither is GPU or scanout time.

Test-first evidence: missing comparator module, then3/3 pure passes; old collector fails30 of34 targeted checks. Initial implementation passed51 combined tests. Self-review added genuine reproductions for conflating absent group material indices with zero and for untracked instanced geometry/scene hooks, then corrected those three causes. `p04d-self-review-red.json` retains the failures; `p04d-self-reviewed-green.json` records56 combined passes. A new test fixture initially passed BoxGeometry to an InstancedBufferGeometry-specific copy API; its type error was corrected by supplying real shared attributes/index and a valid instance count, not by a cast or assertion relaxation. TypeScript now passes.

The initial combined adapter/shadow/bootstrap/vegetation run passed109/109 with Exit0 (`p04-focused-self-verified.json`), followed by four passing browser regressions on that candidate. Those results predate the cache-size correction. They did not establish the static-scene optimization goal. No independent/human review or millisecond improvement is claimed from unit tests. Existing lighting, shaders, AO, map resolution and cast/receive classification remain unchanged; the one old already-hidden-removal assertion now correctly expects no additional redraw.

## Default-scene cache reproduction and correction

Direct production-browser inspection found `shadowCache: budget-exceeded`, `shadowUpdated: true` in the normal128-geometry scene. Rendering stayed correct through conservative fallback, but every static frame rebuilt shadows. The failed live evidence is retained as `p04-static-budget-failure-20260922.json`; the first four E2E passes and visual comparison are not relabelled as proof of reuse.

A new real-Three128-mesh regression failed on the old bound (`p04-default-scale-red.json`). The correction changes only the internal maximum to256KiB; the existing ledger formula automatically charges both possible snapshots. The overflow fixture now exceeds the new bound and still proves no partial cache is retained. Fresh40 shadow/15 batch/2 existing visual tests pass57/57 (`p04-default-scale-green.json`), TypeScript/diff-check pass, and the production build passes with271 modules and its existing chunk warning.

The original live reproduction now passes: complete134496-byte revision, `shadowCache: reused`, `shadowUpdated: false`,169 render calls/374632 triangles, native Running and no console/measurement errors. After a real C01 camera-button action, three spaced diagnostics observations still prove reuse. This observes the existing60-frame diagnostic projection, not every frame or GPU/scanout time. Combined controlled CPU248775768 stays below268435456 with the512KiB shadow reserve and262144-byte optional main observer accounted. Raw evidence: `p04-static-budget-corrected-20260922.json`.

Corrected raw hashes: shadow helper `01bd82481d010a2e96cfdfeba2a5a1cf68b2388c`, visual renderer `f6ba95791cceca404404864b621b17e61b89d791`, shadow test `0895e2e2e731c929e32e2bbdfeb1cb52cd58408e`. The four corrected-candidate E2Es pass4/4 with actual Exit0, one worker/zero retries and no skipped/flaky tests (`p04-after-corrected-01/report.json`). Owned static-probe tab/preview and test runner were closed, foreign blank tab preserved, and port5177 has no listener. The complete corrected unit regression finished with204 files/2246 tests PASS, zero skipped,1041.84s and actual Exit0 (`p04-full-unit-self-verified.json`). No source/test files changed during that baseline.

## Corrected visual evidence

`p04-visual-comparison-corrected-01/manifest.json` binds six named before/after capture pairs to their original reports, dimensions and SHA-256 values. Images are the original1920x1080 PNG bytes, not resized or regenerated Goldens. The corrected quarry-after and rock-before pairs are byte-identical. The other eight images were visually inspected: the coast, vegetation and lighting remain consistent; falling/sleeping/restored fragments are present, but their physical pose or camera position differs at capture time. These dynamic pairs are not aligned pixel-equivalence proof. The original before-run failure remains retained, and its failed case was not selected as a successful baseline.

Visual self-review found no broad style or shadow loss in these captures. It does not establish visual Owner acceptance, every-frame/GPU correctness, the Recovery visual matrix or a whole-Cut performance gain. Those limits remain explicit. All artifacts stay local; independent/human review was NOT RUN.
