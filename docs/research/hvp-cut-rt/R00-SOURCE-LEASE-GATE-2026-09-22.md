# R00 source/lease handoff — P07 gate, 2026-09-22

## Fixed source versus working snapshot

Repository: `https://github.com/BenjaminHornung/Weltraum-Spiel.git`; task branch: `feature/hvp-cut-rt-p00`. Execution base commit `9341fb906383515057e659a99e16a381632f2bea`, tree `98ab58173ef0a61cb2d35cbdc39ab0343f9bce96`. Historical research/main merge commit `25bc7f5bbd2db6317c42193873eadeaf10a092c5` has the same tree. **Neither base commit contains the Cut RT optimization overlay.** This extract was written when the overlay was `UNCOMMITTED_NOT_TRANSFERRED`; that describes the *historical handoff moment*, not the status of a later branch publication. A reader must inspect the branch's current 40-character HEAD and complete tree/file list rather than assign overlay files to the base SHA.

At this gate the modified-file Git blob IDs were `src/hvp/hvpBootstrap.ts` `8b36ee08d41c122b2d0088302bf78797c04da169`, `tests/unit/hvp-bootstrap.test.ts` `2942082db8b5d3d05822418639d53c8b9c84ebe7`, `tests/performance/hvp-cut-rt.spec.ts` `b224055b248f87cc72163b7cea7d4d478d3c62df` (all relative to `apps/weltraum-browser/`). These fingerprints identify file contents, not a candidate commit or published snapshot. The optional external R00 planner should use a fixed Git SHA or another explicit immutable source binding, not a live-mutating worktree.

## Cards, evidence and leases at the handoff

M00 qualified using the then-approved profile. P00 source/Cold and bounded main observation were checked; detailed P00-D worker spans were expressly deferred by the Owner. P01 passed 200 files/1924 units, P02 201/2134, P03 202/2191 with V1/V2 wire parity (the real worker RT speed diagnostic target was **not** met), P04 204/2246 plus actual shadow reuse and 4/4 E2Es, and P05 205/2271 plus 6/6 real browser flows. Corresponding `P00-RESULT.md`–`P05-RESULT.md` in this directory document scope and limits. The combined unchanged full rerun passed 206 files/2343 tests; an earlier full run failed a five-second timeout in an unchanged Rapier test, and its separate failure report was preserved.

The active primary execution orchestrator held only the bounded Restore-/Recut product path `apps/weltraum-browser/src/hvp/hvpBootstrap.ts`, its existing unit file `tests/unit/hvp-bootstrap.test.ts`, performance runner `tests/performance/hvp-cut-rt.spec.ts` and local result documentation. Earlier package writer leases were closed; there was **no transferable R00 product lease**. The narrow fix uses fresh staged *render-only* aliases for terrain, branch and body; canonical owners, save data, source revisions, native physics, admission caps and the backend tombstone guard stay unchanged. The unit fixture reproduced equal-revision tombstone rejection before the fix, then covered real Three-backend rollback/retry and alias/native/source/pose binding. Subsequent real Warm hot-load/recut and four functional browser flows passed. Two independent bounded technical reviews and affected follow-ups found no remaining confirmed blocker in the tested body path. Crafted but schema-valid terrain/foliage alias collisions and a full cold-restored colliding-resident fixture remain LOW/unproven; **do not claim blanket arbitrary-save acceptance**.

P07's predeclared seven variants × Cold/Warm **14/14 diagnostic** browser cases passed with one real Applied cut in each; each retained source/build/browser/GPU/power/viewport bindings and its own input/committed-render records. Individual Input→Applied samples: quarry 231–313 ms, rock arm 1871–2086 ms, body 2629–3659 ms. **n=1 per variant/temperature is not a p95.** Rock-arm Cold showed a `PrepareTerrain` handler/recipe/timer gap of 231.9/230/236 ms and actual Hold 357.7 ms; the old P06 no-recipe-caused-gap>20ms prerequisite is not satisfied. Body Hold is not measured. Full ≥100-per-class/temperature × ≥3-session acceptance, fault/recovery matrix and Visual Owner gate remain open. The prospective, separately reviewed [cooperative follow-up plan](CUT-COOPERATIVE-FOLLOWUP-PLAN.md) is **planning only**, not a new implementation lease or contract approval. The subsequent Owner-requested GitHub branch snapshot includes [selected raw JSON reports and their SHA-256/redaction manifest](../../../apps/weltraum-browser/evidence/hvp-cut-rt-v2/README.md), including the [predeclared diagnostic matrix](P07-DIAGNOSTIC-MATRIX-01.md). Separate PNG and trace archives remain local; the pre-commit source hashes inside reports are historical, not the base commit's contents.

The Owner authorized the current Cut RT implementation/tests and the narrow Restore-/Recut fix, and subsequently explicitly requested a GitHub branch snapshot including these Markdown documents and selected audited JSON reports. This does **not** authorize a main merge, release, deployment, schema/native contract change, R00 Core implementation or publishing the excluded private captures/archives. R00 was not launched by this execution orchestrator; whether the Owner independently started it cannot be inferred. R00 must not depend on a live dirty tree, and ongoing Hestia execution does not wait for an R00 architecture report.
